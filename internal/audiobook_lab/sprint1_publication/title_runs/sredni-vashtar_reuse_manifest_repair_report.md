# Sredni Vashtar Reuse Manifest Repair

Generated: `2026-07-14T09:33:44Z`

## Decision

`RELEASE_SOURCE_APPROVED_PRODUCTION_VALIDATION_PENDING`

The canonical reuse candidate `candidate-61355c7842dbcbae` passed the current release gates without new TTS generation. Its MP3 SHA-256 is `2b328a80b90684ddf2fe3df1a1447481067c6cb277484f97432e882c7844d31a`; the bound sanitized source SHA-256 is `44e3bebedecc69c907b8739b5c6996932505df2cb140c05a4d55b9ca9d2bfd21`.

## Measured Quality

- ASR/source: `9.8426` against the required `9.7` minimum.
- First and last checks: `PASS`.
- Six listening samples: `9.4, 9.4, 9.4, 9.5, 9.4, 9.4`.
- Minimum confidence: `0.95`.
- Fatal flags: none.
- Measured alignment score: `9.7997`; `auto_estimated_sync=false`.
- Public copy and manifest mode: `section_following` only. No word-level claim is made.
- Deterministic `10/10` is not claimed.

## Package Repair

The reuse index originally listed no associated sidecars. The canonical release directory contains a complete OpenAI-measured package bound to the approved MP3 and source: `timestamps.json`, `highlight.vtt`, `chapters.json`, and `meta.json`. Older `reused_*` Piper sidecars were excluded.

All five release objects were uploaded to B2 and verified by full remote SHA-256 and byte size. A direct `Range: bytes=0-1023` request returned HTTP `206` and `1,024` bytes. The controlled source packet was generated by `sprint1_release_packet_builder.py`, preserving the reader manifest's fail-closed source contract while enabling public audio only through controlled launch evidence.

## Safety

No TTS, ASR, or listening provider call ran in this closure stage. No audio binary was added to `frontend/public`, `frontend/build`, or git. The failed single-request uploads created no completed object; the final sequential 5 MiB multipart run passed. `paid_tts.lock` was restored byte-for-byte to `active`, holder `none`, with no allowed next holders.

Production API, UI, and proxy validation remain mandatory after PR merge and deployment.
