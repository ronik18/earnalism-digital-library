# EOD Post-Launch Audiobook Acceleration Queue

## 1. Sredni Vashtar Deployment Retry

No additional audio generation is required. Retry the merged backend deployment, then require production manifest approval, proxy `206`, UI Listen evidence, and approved-control regression checks.

```bash
cd /tmp/earnalism-eod-go-live/backend && railway up . --path-as-root --project a8533934-35c4-463e-9f43-577a9ac391ee --environment production --service earnalism --ci --message "Retry Sredni Vashtar approved reuse package b337300 from backend root"
```

## 2. Non-Paid Repair And Preflight

- `bn-066`: run the saved three-chunk Bengali ASR language/configuration calibration before any paid ASR retry.
- `radharani`, `devdas`, `book-edfcf810c5`: continue their saved representative-audition preflights after Bengali campaign gates permit execution.
- `nishkriti`: reconcile the canonical source layer, then run its saved non-paid preflight.
- `jekyll-and-hyde`: retain the hash-bound input and resume only after provider authentication and short-title sequencing allow it.

## 3. Human Narration Or Licensed Import

Use the existing source-bound intake packets for `muchiram-gurer-jibanchorit`, `book-d19e96859f`, `book-f5d593e1f4`, `the-gift-of-the-magi`, `the-tell-tale-heart`, `the-open-window`, `dsires-baby`, `the-cop-and-the-anthem`, `the-last-leaf`, `the-masque-of-the-red-death`, `the-yellow-wallpaper`, `the-monkeys-paw`, and `the-necklace`. Import received narration through the saved per-title command; do not repeat failed provider fingerprints.

## 4. Owner Documents

`pather-panchali` remains reader-public/audio-hidden until the rights, source, and cover document checklist is complete. No paid audio work is allowed first.

## 5. Long-Title Backlog

Keep `dracula`, `frankenstein`, `picture-of-dorian-gray`, `the-time-machine`, `the-call-of-the-wild`, `white-fang`, `pride-and-prejudice`, `the-secret-garden`, and `alices-adventures-in-wonderland` in non-paid preflight until the short-title path produces additional production successes and the coordinator reconfirms budget.
