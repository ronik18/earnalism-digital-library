# A8 self-review

The prior benchmark is retained as valid history and is not used as final authority.
The exact-head three-round benchmark was independently inspected, while the accepted
sanitized backend-integration artifact was preserved and carried forward only because
its own authority is unchanged. No production runtime, data, deployment, or cost claim
was introduced. The final package must be generated after the exact-head freeze and
validated from an independent clean extraction.

Rollback is limited to the A8 evidence/tooling commit range beginning at
`9a9411f1930550b0c87c41f81417133dcf874967`; it preserves the A1-A7 runtime commits.
