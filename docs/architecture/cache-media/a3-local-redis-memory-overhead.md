# A3 local Redis memory overhead

`redis-server` is unavailable on this host, so no observed `MEMORY USAGE` result is claimed. The capacity model uses a deterministic structural estimate of key bytes plus stored envelope bytes plus 128 bytes of Redis object overhead. No production Redis was contacted.
