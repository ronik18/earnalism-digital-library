# A4 mutation and invalidation map

PASS. The six active policies have complete current dependency coverage. Public and reader-content values are invalidated through the existing generation advancement after source mutations. Reader manifests use their resource/version identity. User document, session, transaction, and payment read models use exact v2 deletion through `_invalidate_user_cache` after existing source mutations. Nonactive audio, PDF, signed-delivery, and entitlement paths remain deferred and are not cache candidates.
