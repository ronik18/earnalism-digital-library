# A4 coherence contract

PASS. Redis remains optional. Protected authorization remains authoritative. A cache-aside loader compares its coherence version before and after source loading; a changed version suppresses its cache write. Local flights use a nonlogged SHA-256 internal identity of policy, v2 key, and version. Distributed coalescing is not justified without live source-cost and traffic evidence, so it is not enabled.
