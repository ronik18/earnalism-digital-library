# A8 artifact path exposure

The prior successful artifact was withheld after one GitHub-hosted runner workspace-path exposure was found in a diagnostic warning log. Raw logs now remain ephemeral and the published envelope contains only deterministic sanitized copies. No production logging behavior changed.
