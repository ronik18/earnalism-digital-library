# Brand lockup placement inventory

Decision: `EARNALISM_SEAMLESS_PAPER_MASTHEAD_V1`.

The canonical lockup is rendered only by `EarnalismBrandLockup`; the wrapper is transparent and its parent supplies archival paper.

See the paired JSON inventory for searchable placement classifications.
