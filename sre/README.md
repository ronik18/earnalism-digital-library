# Earnalism SRE quarantine bootstrap

This directory contains non-authoritative operational state only.

`sre/INVENTORY.yaml` and `sre/AUTONOMY_POLICY.yaml` are intentionally absent. They are operator-owned authority controls and must not be invented by an autonomous agent. Until they are restored, these artifacts cannot authorize production inspection, mutation, deployment, or target expansion.

All observations in this directory are marked `observed_only` or `draft_non_authoritative`. Production release remains blocked fail-closed.

