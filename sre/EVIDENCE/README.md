# Evidence

Evidence must be attached to each finding with timestamp, commit, environment, and tool version. The quarantine bootstrap records command-level evidence in `STATE.json` and `RISK_REGISTER.json`; it does not claim telemetry, backup-restore, canary, or rollback evidence that was not observed.
