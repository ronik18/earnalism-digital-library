web: uvicorn server:app --host 0.0.0.0 --port ${PORT:-8000} --workers ${WEB_CONCURRENCY:-2} --proxy-headers --forwarded-allow-ips '*' --timeout-keep-alive ${UVICORN_KEEP_ALIVE:-15} --timeout-graceful-shutdown ${UVICORN_GRACEFUL_TIMEOUT:-15}
