{
  "id": "chapter-006",
  "title": "Chapter 6: Tool-Using Agents in Python",
  "order": 6,
  "content": "<h1>Chapter 6: Tool-Using Agents in Python</h1><h2>Opening Hook: From Talking About Work to Doing Work</h2><p>A chatbot can talk about a calculation.</p><p>A tool-using agent can actually calculate.</p><p>A chatbot can explain what an API is.</p><p>A tool-using agent can call an API.</p><p>A chatbot can tell you how to summarize text.</p><p>A tool-using agent can take the text, run the right function, and return the summary.</p><p>This is the chapter where your AI assistant starts to feel more like an agent.</p><p>So far, your projects have mostly focused on understanding, generating, and retrieving text.</p><p>You built:</p><ul><li>A code explainer</li><li>A reusable LLM client</li><li>A command-line assistant</li><li>A PDF RAG assistant</li><li>A semantic search engine</li></ul><p>Now you will add a new ability:</p><blockquote><p>Tools.</p></blockquote><p>A tool is a Python function your agent can use.</p><p>That simple idea changes everything.</p><p>Instead of relying only on the model, your app can combine model reasoning with actual software actions.</p><p>In this chapter, you will build your second flagship project:</p><p><strong>API Tool-Using Agent</strong></p><p>It will choose from a small set of tools, run the selected tool, and then use the LLM to explain the result clearly.</p><hr><h2>What You Will Learn</h2><p>By the end of this chapter, you will understand:</p><ul><li>What tool calling means</li><li>Why tools make AI agents more useful</li><li>How Python functions become agent tools</li><li>How to design safe tool inputs</li><li>How to build a tool router</li><li>How to create calculator, summarizer, and API tools</li><li>How to validate tool names and tool inputs</li><li>How to log tool calls</li><li>Why unrestricted tools are dangerous</li><li>How this pattern connects to production agent frameworks</li></ul><hr><h2>Why This Chapter Matters</h2><p>An AI model is powerful, but it has limits.</p><p>It may not know live data.</p><p>It may make math mistakes.</p><p>It cannot safely access your private APIs unless your app gives it controlled access.</p><p>It cannot update databases, send emails, or check files by itself.</p><p>Tools solve part of this problem.</p><p>A tool gives your agent a controlled way to interact with software.</p><p>For example:</p><table><thead><tr><th>User Request</th><th>Tool Needed</th></tr></thead><tbody><tr><td>“What is 245 × 18?”</td><td>Calculator</td></tr><tr><td>“Summarize this paragraph.”</td><td>Summarizer</td></tr><tr><td>“What is my public IP?”</td><td>API lookup</td></tr><tr><td>“Find notes about RAG.”</td><td>Semantic search</td></tr><tr><td>“Search this PDF.”</td><td>RAG retriever</td></tr><tr><td>“Create a task list.”</td><td>Task planner</td></tr></tbody></table><p>Modern AI APIs and agent frameworks support tool calling patterns, but the core idea is simple:</p><blockquote><p>The model can help decide what action is needed, and your application controls which tool actually runs.</p></blockquote><p>In this chapter, you will build that control layer yourself.</p><p>Framework magic is easier to understand after you know the underlying pattern.</p><hr><h2>6.1 What Is a Tool?</h2><p>A tool is a function that performs one clear action.</p><p>Example:</p><pre><code>def add_numbers(a: int, b: int) -&gt; int:\n return a + b</code></pre><p>For an AI agent, a tool might be:</p><p>def calculator(expression: str) -&gt; str:<br>...</p><p>or:</p><p>def summarize_text(text: str) -&gt; str:<br>...</p><p>or:</p><pre><code>def get_public_ip(_: str = &quot;&quot;) -&gt; str:\n ...</code></pre><p>The model does not directly control your computer.</p><p>Your Python app controls the tools.</p><p>That distinction is important.</p><p>A safe tool-using agent should follow this pattern:</p><p>User request<br>↓<br>Tool selection<br>↓<br>Input validation<br>↓<br>Tool execution<br>↓<br>Tool result<br>↓<br>Final response</p><p>The model may help select a tool, but your code decides what tools exist, what inputs are allowed, and what result is returned.</p><hr><h2>6.2 Why Tools Matter</h2><p>Without tools, an LLM can only generate text.</p><p>With tools, your app can:</p><ul><li>Calculate</li><li>Search</li><li>Retrieve documents</li><li>Query APIs</li><li>Read files</li><li>Format data</li><li>Create reports</li><li>Trigger workflows</li><li>Ask for human approval before actions</li></ul><p>Tools turn an AI assistant from a text generator into a task helper.</p><p>But tools also introduce risk.</p><p>A poorly designed tool can:</p><ul><li>Leak private data</li><li>Delete important files</li><li>Send unwanted emails</li><li>Make expensive API calls</li><li>Run unsafe code</li><li>Produce wrong results</li><li>Take actions without approval</li></ul><p>So the goal is not:</p><p>Give the AI every tool.</p><p>The goal is:</p><p>Give the agent narrow, safe, useful tools with clear rules.</p><hr><h2>6.3 Tool Calling vs Function Calling</h2><p>You may see two common phrases:</p><ul><li>Tool calling</li><li>Function calling</li></ul><p>In many AI development conversations, these ideas are closely related.</p><p>A function is a piece of code.</p><p>A tool is a function exposed to an AI system in a controlled way.</p><p>For example, this is a regular function:</p><pre><code>def word_count(text: str) -&gt; int:\n return len(text.split())</code></pre><p>It becomes an agent tool when your app allows the AI workflow to choose and run it.</p><p>A tool usually needs:</p><ul><li>A name</li><li>A description</li><li>Input rules</li><li>Output format</li><li>Safety limits</li></ul><p>In beginner projects, you can build a manual tool router.</p><p>In advanced projects, you can use provider-supported tool calling or an agent framework.</p><p>This chapter uses a manual router first.</p><p>That keeps the logic visible.</p><hr><h2>6.4 The Manual Tool Router Pattern</h2><p>A manual tool router asks the model to choose a tool from an approved list.</p><p>The model returns structured text, usually JSON.</p><p>Your Python app parses the JSON, checks whether the tool is allowed, runs the tool, and returns the result.</p><p>The flow looks like this:</p><p>User request<br>↓<br>LLM chooses tool from approved list<br>↓<br>Python validates tool choice<br>↓<br>Python runs the tool<br>↓<br>Tool result returns to app<br>↓<br>LLM writes final answer</p><p>Important:</p><blockquote><p>The model suggests. Your code decides.</p></blockquote><p>This prevents the model from inventing tools or running unsafe actions.</p><hr><h2>6.5 Project: API Tool-Using Agent</h2><h3>What This Project Does</h3><p>This project builds an agent that can use three tools:</p><ol><li>Calculator tool</li><li>Text summarizer tool</li><li>Public IP lookup API tool</li></ol><p>The user can type:</p><p>What is 245 * 18?</p><p>The agent chooses the calculator tool.</p><p>The user can type:</p><p>Summarize this: Python is used for automation, AI, and web development.</p><p>The agent chooses the summarizer tool.</p><p>The user can type:</p><p>What is my public IP?</p><p>The agent chooses the API lookup tool.</p><h3>Who This Helps</h3><p>This project helps:</p><ul><li>Python learners understand tool calling</li><li>Developers build safer AI assistants</li><li>Freelancers prototype business automation tools</li><li>Students create stronger portfolio projects</li><li>Future chapters that need tools, workflows, and guardrails</li></ul><h3>What You Will Build</h3><p>Create this project structure:</p><p>chapter_06_tool_agent/<br>tools/<br>__init__.py<br>calculator.py<br>api_tools.py<br>summarizer.py<br>.env.example<br>.gitignore<br>requirements.txt<br>llm_client.py<br>tool_registry.py<br>tool_router.py<br>agent.py<br>app.py<br>test_calculator.py<br>test_tool_registry.py<br>test_tool_router.py<br>README.md</p><hr><h2>6.6 Setup</h2><h3>Step 1: Create the Folder</h3><p>mkdir chapter_06_tool_agent<br>cd chapter_06_tool_agent<br>mkdir tools</p><h3>Step 2: Create a Virtual Environment</h3><p>python -m venv .venv</p><p>Activate it.</p><p>On macOS or Linux:</p><p>source .venv/bin/activate</p><p>On Windows PowerShell:</p><p>.venv\\Scripts\\Activate.ps1</p><h3>Step 3: Create requirements.txt</h3><p>openai<br>python-dotenv<br>pytest<br>requests</p><p>Install dependencies:</p><p>pip install -r requirements.txt</p><h3>Step 4: Create .env.example</h3><p>OPENAI_API_KEY=replace_with_your_api_key<br>OPENAI_MODEL=gpt-4.1-mini<br>OPENAI_TEMPERATURE=0.2</p><p>Copy it to .env.</p><p>On macOS or Linux:</p><p>cp .env.example .env</p><p>On Windows PowerShell:</p><p>Copy-Item .env.example .env</p><p>Then edit .env and add your real API key.</p><p>Never publish your real .env file.</p><hr><h2>6.7 File: .gitignore</h2><p>Create .gitignore:</p><p>.env<br>.venv/<br>__pycache__/<br>.pytest_cache/<br>*.pyc<br>*.log</p><hr><h2>6.8 File: tools/<strong>init</strong>.py</h2><p>Create tools/__init__.py:</p><p>&quot;&quot;&quot;<br>Tool package for Chapter 6.<br>&quot;&quot;&quot;</p><p>Not executed automatically: requires live API credentials, network access, or external service.</p><p>This makes the tools folder importable as a Python package.</p><hr><h2>6.9 File: llm_client.py</h2><p>Create llm_client.py:</p><p>&quot;&quot;&quot;<br>Chapter 6: Reusable LLM Client<br>&quot;&quot;&quot;</p><pre><code>import os\nfrom dataclasses import dataclass, field\nfrom dotenv import load_dotenv\nfrom openai import OpenAI</code></pre><p>load_dotenv()</p><p>def get_env_float(name: str, default: float) -&gt; float:<br>&quot;&quot;&quot;Read a float from environment variables safely.&quot;&quot;&quot;</p><p>raw_value = os.getenv(name)</p><p>if raw_value is None:<br>return default</p><pre><code>try:\n return float(raw_value)\n except ValueError:\n return default</code></pre><p>@dataclass<br>class LLMConfig:<br>&quot;&quot;&quot;Configuration for the LLM client.&quot;&quot;&quot;</p><pre><code>model: str = field(\n default_factory=lambda: os.getenv(&quot;OPENAI_MODEL&quot;, &quot;gpt-4.1-mini&quot;)\n )\n temperature: float = field(\n default_factory=lambda: get_env_float(&quot;OPENAI_TEMPERATURE&quot;, 0.2)\n )</code></pre><p>class LLMClient:<br>&quot;&quot;&quot;Small reusable client for sending prompts to an LLM.&quot;&quot;&quot;</p><pre><code>def __init__(self, config: LLMConfig | None = None):\n self.config = config or LLMConfig()\n api_key = os.getenv(&quot;OPENAI_API_KEY&quot;)</code></pre><p>if not api_key:<br>raise ValueError(<br>&quot;OPENAI_API_KEY is missing. Add it to your .env file.&quot;<br>)</p><p>self.client = OpenAI(api_key=api_key)</p><p>def complete(self, system: str, user: str) -&gt; str:<br>&quot;&quot;&quot;Send a request to the model and return text output.&quot;&quot;&quot;</p><pre><code>if not system.strip():\n raise ValueError(&quot;System instruction cannot be empty.&quot;)</code></pre><pre><code>if not user.strip():\n raise ValueError(&quot;User request cannot be empty.&quot;)</code></pre><p>model_input = f&quot;&quot;&quot;<br>System instruction:<br>{system.strip()}</p><p>User request:<br>{user.strip()}<br>&quot;&quot;&quot;</p><p>response = self.client.responses.create(<br>model=self.config.model,<br>input=model_input,<br>temperature=self.config.temperature,<br>)</p><p>return response.output_text</p><p>Not executed automatically: requires live API credentials, network access, or external service.</p><hr><h2>6.10 File: tools/calculator.py</h2><p>A calculator tool sounds simple, but it can be dangerous if implemented badly.</p><p>Many beginners write this:</p><p>eval(user_input)</p><p>That is risky.</p><p>eval() can execute arbitrary Python code if user input is malicious.</p><p>For this project, you will build a safer calculator using Python’s ast module.</p><p>Create tools/calculator.py:</p><p>&quot;&quot;&quot;<br>Safe calculator tool for the Chapter 6 tool-using agent.</p><p>This tool supports:<br>- Addition<br>- Subtraction<br>- Multiplication<br>- Division<br>- Exponents<br>- Parentheses<br>- Unary plus and minus</p><p>It does not execute arbitrary Python code.<br>&quot;&quot;&quot;</p><pre><code>import ast\nimport operator</code></pre><p>ALLOWED_OPERATORS = {<br>ast.Add: operator.add,<br>ast.Sub: operator.sub,<br>ast.Mult: operator.mul,<br>ast.Div: operator.truediv,<br>ast.Pow: operator.pow,<br>ast.USub: operator.neg,<br>ast.UAdd: operator.pos,<br>}</p><p>def evaluate_node(node):<br>&quot;&quot;&quot;Recursively evaluate a safe arithmetic AST node.&quot;&quot;&quot;</p><p>if isinstance(node, ast.Constant) and isinstance(node.value, (int, float)):<br>return node.value</p><pre><code>if isinstance(node, ast.BinOp):\n left = evaluate_node(node.left)\n right = evaluate_node(node.right)\n operator_type = type(node.op)</code></pre><p>if operator_type not in ALLOWED_OPERATORS:<br>raise ValueError(&quot;Unsupported arithmetic operator.&quot;)</p><p>return ALLOWED_OPERATORS[operator_type](left, right)</p><pre><code>if isinstance(node, ast.UnaryOp):\n operand = evaluate_node(node.operand)\n operator_type = type(node.op)</code></pre><p>if operator_type not in ALLOWED_OPERATORS:<br>raise ValueError(&quot;Unsupported unary operator.&quot;)</p><p>return ALLOWED_OPERATORS[operator_type](operand)</p><p>raise ValueError(&quot;Unsupported expression.&quot;)</p><p>def calculator(expression: str) -&gt; str:<br>&quot;&quot;&quot;<br>Evaluate a basic arithmetic expression safely.</p><p>Args:<br>expression: Arithmetic expression as text.</p><p>Returns:<br>The calculated result as a string.<br>&quot;&quot;&quot;</p><pre><code>if not expression.strip():\n return &quot;Calculator error: expression cannot be empty.&quot;</code></pre><p>if len(expression) &gt; 200:<br>return &quot;Calculator error: expression is too long.&quot;</p><pre><code>try:\n parsed = ast.parse(expression, mode=&quot;eval&quot;)\n result = evaluate_node(parsed.body)\n return str(result)\n except ZeroDivisionError:\n return &quot;Calculator error: division by zero.&quot;\n except Exception as error:\n return f&quot;Calculator error: {error}&quot;</code></pre><p>Test result: Python syntax check passed.</p><hr><h2>6.11 File: tools/api_tools.py</h2><p>This file contains a simple public API lookup tool.</p><p>Create tools/api_tools.py:</p><p>&quot;&quot;&quot;<br>API tools for the Chapter 6 tool-using agent.<br>&quot;&quot;&quot;</p><p>import requests</p><pre><code>def get_public_ip(_: str = &quot;&quot;) -&gt; str:\n &quot;&quot;&quot;\n Fetch the public IP address using a simple public API.</code></pre><p>The input argument is ignored so the tool has a consistent signature.<br>&quot;&quot;&quot;</p><p>url = &quot;https://api.ipify.org?format=json&quot;</p><pre><code>try:\n response = requests.get(url, timeout=10)\n response.raise_for_status()\n data = response.json()\n ip_address = data.get(&quot;ip&quot;)</code></pre><p>if not ip_address:<br>return &quot;API error: response did not include an IP address.&quot;</p><p>return f&quot;Public IP address: {ip_address}&quot;</p><pre><code>except requests.Timeout:\n return &quot;API error: request timed out.&quot;\n except requests.RequestException as error:\n return f&quot;API error: {error}&quot;\n except ValueError:\n return &quot;API error: response was not valid JSON.&quot;</code></pre><p>Not executed automatically: requires live API credentials, network access, or external service.</p><p>External API calls should use timeouts so your app does not wait forever if a service is slow or unavailable.</p><hr><h2>6.12 File: tools/summarizer.py</h2><p>The summarizer tool uses the LLM client.</p><p>This is a tool that calls the model again.</p><p>That is allowed, but remember that each model call can affect cost and latency.</p><p>Create tools/summarizer.py:</p><p>&quot;&quot;&quot;<br>Text summarizer tool for the Chapter 6 tool-using agent.<br>&quot;&quot;&quot;</p><p>from llm_client import LLMClient</p><p>def summarize_text(text: str) -&gt; str:<br>&quot;&quot;&quot;<br>Summarize text in three bullet points.<br>&quot;&quot;&quot;</p><pre><code>if not text.strip():\n return &quot;Summarizer error: text cannot be empty.&quot;</code></pre><p>if len(text) &gt; 5000:<br>return &quot;Summarizer error: text is too long for this beginner demo.&quot;</p><p>client = LLMClient()</p><p>return client.complete(<br>system=&quot;You summarize text clearly and accurately.&quot;,<br>user=f&quot;&quot;&quot;<br>Summarize the following text in three bullet points.</p><p>Text:<br>{text}</p><p>Rules:<br>- Do not add facts that are not present.<br>- Keep the summary concise.<br>&quot;&quot;&quot;,<br>)</p><p>Test result: Python syntax check passed.</p><hr><h2>6.13 File: tool_registry.py</h2><p>The tool registry is a list of approved tools.</p><p>This is important.</p><p>The agent should not be able to call any Python function.</p><p>It should only call tools you approve.</p><p>Create tool_registry.py:</p><p>&quot;&quot;&quot;<br>Tool registry for the Chapter 6 tool-using agent.<br>&quot;&quot;&quot;</p><pre><code>from dataclasses import dataclass\nfrom typing import Callable</code></pre><pre><code>from tools.calculator import calculator\nfrom tools.api_tools import get_public_ip\nfrom tools.summarizer import summarize_text</code></pre><p>ToolFunction = Callable[[str], str]</p><p>@dataclass<br>class Tool:<br>&quot;&quot;&quot;A tool the agent is allowed to use.&quot;&quot;&quot;</p><p>name: str<br>description: str<br>function: ToolFunction</p><p>TOOLS: dict[str, Tool] = {<br>&quot;calculator&quot;: Tool(<br>name=&quot;calculator&quot;,<br>description=&quot;Use for basic arithmetic expressions such as 245 * 18.&quot;,<br>function=calculator,<br>),<br>&quot;summarize_text&quot;: Tool(<br>name=&quot;summarize_text&quot;,<br>description=&quot;Use for summarizing user-provided text.&quot;,<br>function=summarize_text,<br>),<br>&quot;get_public_ip&quot;: Tool(<br>name=&quot;get_public_ip&quot;,<br>description=&quot;Use when the user asks for the public IP address.&quot;,<br>function=get_public_ip,<br>),<br>}</p><p>def get_tool_descriptions() -&gt; str:<br>&quot;&quot;&quot;<br>Return tool descriptions for the router prompt.<br>&quot;&quot;&quot;</p><p>lines = []</p><p>for tool in TOOLS.values():<br>lines.append(f&quot;- {tool.name}: {tool.description}&quot;)</p><p>return &quot;\\n&quot;.join(lines)</p><p>def is_allowed_tool(tool_name: str) -&gt; bool:<br>&quot;&quot;&quot;<br>Check whether the requested tool is approved.<br>&quot;&quot;&quot;</p><p>return tool_name in TOOLS</p><p>def run_tool(tool_name: str, tool_input: str) -&gt; str:<br>&quot;&quot;&quot;<br>Run an approved tool.<br>&quot;&quot;&quot;</p><p>if not is_allowed_tool(tool_name):<br>return f&quot;Tool error: '{tool_name}' is not an allowed tool.&quot;</p><pre><code>tool = TOOLS[tool_name]\n return tool.function(tool_input)</code></pre><p>Test result: Python syntax check passed.</p><hr><h2>6.14 File: tool_router.py</h2><p>This file asks the model to choose the best tool.</p><p>The model must return JSON.</p><p>Then your Python code parses and validates it.</p><p>Create tool_router.py:</p><p>&quot;&quot;&quot;<br>Tool router for the Chapter 6 tool-using agent.</p><p>The router asks the LLM to choose one approved tool.<br>&quot;&quot;&quot;</p><pre><code>import json\nfrom dataclasses import dataclass</code></pre><pre><code>from llm_client import LLMClient\nfrom tool_registry import get_tool_descriptions, is_allowed_tool</code></pre><p>ROUTER_SYSTEM_PROMPT = &quot;&quot;&quot;<br>You are a tool router for a Python AI agent.</p><p>Your job is to choose the best tool for the user's request.</p><p>You must choose only from the approved tools.<br>Return only valid JSON.<br>Do not include Markdown.<br>Do not explain your decision outside JSON.<br>&quot;&quot;&quot;</p><p>@dataclass<br>class ToolDecision:<br>&quot;&quot;&quot;The tool chosen by the router.&quot;&quot;&quot;</p><p>tool_name: str<br>tool_input: str<br>reason: str</p><p>def build_router_prompt(user_request: str) -&gt; str:<br>&quot;&quot;&quot;<br>Build a prompt asking the model to choose a tool.<br>&quot;&quot;&quot;</p><p>tool_descriptions = get_tool_descriptions()</p><p>return f&quot;&quot;&quot;<br>Approved tools:<br>{tool_descriptions}</p><p>User request:<br>{user_request}</p><p>Return only JSON with this structure:<br>{{<br>&quot;tool_name&quot;: &quot;calculator | summarize_text | get_public_ip&quot;,<br>&quot;tool_input&quot;: &quot;input to pass to the tool&quot;,<br>&quot;reason&quot;: &quot;short reason for choosing this tool&quot;<br>}}</p><p>Rules:<br>- Use calculator only for arithmetic.<br>- Use summarize_text only when the user asks to summarize text.<br>- Use get_public_ip only when the user asks for public IP.<br>- If the request contains arithmetic, tool_input should be only the expression.<br>- If the request asks for summarization, tool_input should be the text to summarize.<br>- If the request asks for public IP, tool_input can be an empty string.<br>&quot;&quot;&quot;</p><p>def parse_tool_decision(raw_text: str) -&gt; ToolDecision:<br>&quot;&quot;&quot;<br>Parse the router's JSON output into a ToolDecision.<br>&quot;&quot;&quot;</p><pre><code>try:\n data = json.loads(raw_text)\n except json.JSONDecodeError as error:\n raise ValueError(f&quot;Router did not return valid JSON: {error}&quot;) from error</code></pre><pre><code>tool_name = str(data.get(&quot;tool_name&quot;, &quot;&quot;)).strip()\n tool_input = str(data.get(&quot;tool_input&quot;, &quot;&quot;)).strip()\n reason = str(data.get(&quot;reason&quot;, &quot;&quot;)).strip()</code></pre><p>if not tool_name:<br>raise ValueError(&quot;Router response is missing tool_name.&quot;)</p><p>if not is_allowed_tool(tool_name):<br>raise ValueError(f&quot;Router chose an unsupported tool: {tool_name}&quot;)</p><p>return ToolDecision(<br>tool_name=tool_name,<br>tool_input=tool_input,<br>reason=reason or &quot;No reason provided.&quot;,<br>)</p><p>def choose_tool(user_request: str) -&gt; ToolDecision:<br>&quot;&quot;&quot;<br>Ask the LLM to choose a tool for the user request.<br>&quot;&quot;&quot;</p><pre><code>if not user_request.strip():\n raise ValueError(&quot;User request cannot be empty.&quot;)</code></pre><pre><code>client = LLMClient()\n prompt = build_router_prompt(user_request)</code></pre><p>raw_response = client.complete(<br>system=ROUTER_SYSTEM_PROMPT,<br>user=prompt,<br>)</p><p>return parse_tool_decision(raw_response)</p><p>Test result: Python syntax check passed.</p><hr><h2>6.15 File: agent.py</h2><p>The agent combines everything.</p><p>It:</p><ol><li>Receives a user request</li><li>Chooses a tool</li><li>Runs the tool</li><li>Sends the tool result back to the LLM</li><li>Returns a final answer</li></ol><p>Create agent.py:</p><p>&quot;&quot;&quot;<br>Tool-using agent for Chapter 6.<br>&quot;&quot;&quot;</p><pre><code>from llm_client import LLMClient\nfrom tool_registry import run_tool\nfrom tool_router import choose_tool, ToolDecision</code></pre><p>FINAL_ANSWER_SYSTEM_PROMPT = &quot;&quot;&quot;<br>You are a practical AI assistant.</p><p>You receive:<br>1. The user's original request<br>2. The tool that was used<br>3. The tool result</p><p>Write a clear final answer for the user.</p><p>Rules:<br>- Do not invent tool results.<br>- If the tool returned an error, explain it simply.<br>- Keep the answer concise.<br>&quot;&quot;&quot;</p><p>def build_final_answer_prompt(<br>user_request: str,<br>decision: ToolDecision,<br>tool_result: str,<br>) -&gt; str:<br>&quot;&quot;&quot;<br>Build the prompt for the final user-facing answer.<br>&quot;&quot;&quot;</p><p>return f&quot;&quot;&quot;<br>User request:<br>{user_request}</p><p>Tool selected:<br>{decision.tool_name}</p><p>Router reason:<br>{decision.reason}</p><p>Tool input:<br>{decision.tool_input}</p><p>Tool result:<br>{tool_result}</p><p>Write the final answer for the user.<br>&quot;&quot;&quot;</p><p>def run_agent(user_request: str) -&gt; str:<br>&quot;&quot;&quot;<br>Run the full tool-using agent workflow.<br>&quot;&quot;&quot;</p><pre><code>if not user_request.strip():\n return &quot;Please enter a request.&quot;</code></pre><pre><code>try:\n decision = choose_tool(user_request)\n except Exception as error:\n return f&quot;Tool routing error: {error}&quot;</code></pre><p>tool_result = run_tool(<br>tool_name=decision.tool_name,<br>tool_input=decision.tool_input,<br>)</p><pre><code>try:\n client = LLMClient()\n final_prompt = build_final_answer_prompt(\n user_request=user_request,\n decision=decision,\n tool_result=tool_result,\n )</code></pre><p>return client.complete(<br>system=FINAL_ANSWER_SYSTEM_PROMPT,<br>user=final_prompt,<br>)</p><p>except Exception as error:<br>return (<br>&quot;The tool ran, but the final explanation failed.\\n&quot;<br>f&quot;Tool used: {decision.tool_name}\\n&quot;<br>f&quot;Tool result: {tool_result}\\n&quot;<br>f&quot;Explanation error: {error}&quot;<br>)</p><p>Test result: Python syntax check passed.</p><hr><h2>6.16 File: app.py</h2><p>This is the command-line interface.</p><p>Create app.py:</p><p>&quot;&quot;&quot;<br>Chapter 6 Project: API Tool-Using Agent<br>&quot;&quot;&quot;</p><p>from agent import run_agent</p><p>def main() -&gt; None:<br>&quot;&quot;&quot;Run the command-line tool-using agent.&quot;&quot;&quot;</p><p>print(&quot;\\nAPI Tool-Using Agent&quot;)<br>print(&quot;=&quot; * 40)<br>print(&quot;Ask for calculations, summaries, or your public IP.&quot;)<br>print(&quot;Type /exit to quit.&quot;)<br>print(&quot;=&quot; * 40)</p><p>while True:<br>user_request = input(&quot;\\nRequest: &quot;).strip()</p><p>if user_request.lower() in {&quot;/exit&quot;, &quot;exit&quot;, &quot;quit&quot;}:<br>print(&quot;Goodbye.&quot;)<br>break</p><p>if not user_request:<br>print(&quot;Please enter a request.&quot;)<br>continue</p><p>answer = run_agent(user_request)</p><p>print(&quot;\\nAgent:&quot;)<br>print(answer)</p><p>if __name__ == &quot;__main__&quot;:<br>main()</p><p>Test result: Python syntax check passed.</p><hr><h2>6.17 File: test_calculator.py</h2><p>These tests validate the calculator without calling the model API.</p><p>Create test_calculator.py:</p><p>from tools.calculator import calculator</p><pre><code>def test_calculator_addition():\n assert calculator(&quot;2 + 3&quot;) == &quot;5&quot;</code></pre><pre><code>def test_calculator_multiplication():\n assert calculator(&quot;6 * 7&quot;) == &quot;42&quot;</code></pre><pre><code>def test_calculator_parentheses():\n assert calculator(&quot;(2 + 3) * 4&quot;) == &quot;20&quot;</code></pre><pre><code>def test_calculator_division_by_zero():\n result = calculator(&quot;10 / 0&quot;)</code></pre><p>assert &quot;division by zero&quot; in result</p><pre><code>def test_calculator_rejects_unsupported_expression():\n result = calculator(&quot;__import__('os').system('echo unsafe')&quot;)</code></pre><p>assert &quot;Calculator error&quot; in result</p><pre><code>def test_calculator_empty_expression():\n result = calculator(&quot;&quot;)</code></pre><p>assert &quot;cannot be empty&quot; in result</p><p>Test result: Python syntax check passed.</p><hr><h2>6.18 File: test_tool_registry.py</h2><p>Create test_tool_registry.py:</p><p>from tool_registry import (<br>TOOLS,<br>get_tool_descriptions,<br>is_allowed_tool,<br>run_tool,<br>)</p><pre><code>def test_tool_registry_contains_expected_tools():\n assert &quot;calculator&quot; in TOOLS\n assert &quot;summarize_text&quot; in TOOLS\n assert &quot;get_public_ip&quot; in TOOLS</code></pre><pre><code>def test_get_tool_descriptions_mentions_tools():\n descriptions = get_tool_descriptions()</code></pre><pre><code>assert &quot;calculator&quot; in descriptions\n assert &quot;summarize_text&quot; in descriptions\n assert &quot;get_public_ip&quot; in descriptions</code></pre><pre><code>def test_is_allowed_tool():\n assert is_allowed_tool(&quot;calculator&quot;) is True\n assert is_allowed_tool(&quot;delete_files&quot;) is False</code></pre><pre><code>def test_run_allowed_calculator_tool():\n result = run_tool(&quot;calculator&quot;, &quot;10 + 5&quot;)</code></pre><p>assert result == &quot;15&quot;</p><pre><code>def test_run_disallowed_tool():\n result = run_tool(&quot;delete_files&quot;, &quot;anything&quot;)</code></pre><p>assert &quot;not an allowed tool&quot; in result</p><p>Test result: Python syntax check passed.</p><hr><h2>6.19 File: test_tool_router.py</h2><p>These tests check router prompt building and JSON parsing without making an API call.</p><p>Create test_tool_router.py:</p><p>import pytest</p><p>from tool_router import (<br>build_router_prompt,<br>parse_tool_decision,<br>)</p><pre><code>def test_build_router_prompt_includes_user_request():\n prompt = build_router_prompt(&quot;What is 2 + 2?&quot;)</code></pre><pre><code>assert &quot;What is 2 + 2?&quot; in prompt\n assert &quot;Approved tools:&quot; in prompt\n assert &quot;calculator&quot; in prompt</code></pre><pre><code>def test_parse_tool_decision_valid_json():\n raw = &quot;&quot;&quot;\n{\n &quot;tool_name&quot;: &quot;calculator&quot;,\n &quot;tool_input&quot;: &quot;2 + 2&quot;,\n &quot;reason&quot;: &quot;The user asked for arithmetic.&quot;\n}\n&quot;&quot;&quot;</code></pre><p>decision = parse_tool_decision(raw)</p><pre><code>assert decision.tool_name == &quot;calculator&quot;\n assert decision.tool_input == &quot;2 + 2&quot;\n assert &quot;arithmetic&quot; in decision.reason</code></pre><pre><code>def test_parse_tool_decision_invalid_json():\n with pytest.raises(ValueError):\n parse_tool_decision(&quot;not json&quot;)</code></pre><pre><code>def test_parse_tool_decision_rejects_unknown_tool():\n raw = &quot;&quot;&quot;\n{\n &quot;tool_name&quot;: &quot;delete_files&quot;,\n &quot;tool_input&quot;: &quot;/&quot;,\n &quot;reason&quot;: &quot;Unsafe request.&quot;\n}\n&quot;&quot;&quot;</code></pre><p>with pytest.raises(ValueError):<br>parse_tool_decision(raw)</p><p>Test result: Python syntax check passed.</p><hr><h2>6.20 File: README.md</h2><p>Create README.md:</p><p>## Project: API Tool-Using Agent</p><p>This project builds a beginner-friendly tool-using AI agent in Python.</p><p>## Features</p><p>- Manual LLM-based tool router<br>- Safe calculator tool<br>- Text summarizer tool<br>- Public IP API lookup tool<br>- Tool registry<br>- Tool validation<br>- Final answer generation<br>- Non-API unit tests for core logic</p><p>## Setup</p><p>python -m venv .venv</p><p>source .venv/bin/activate</p><p>pip install -r requirements.txt</p><p>cp .env.example .env</p><p>On Windows PowerShell:</p><p>python -m venv .venv</p><p>.venv\\Scripts\\Activate.ps1</p><p>pip install -r requirements.txt</p><p>Copy-Item .env.example .env</p><p>Edit `.env` and add your API key.</p><p>## Run Tests</p><p>pytest</p><p>## Run the App</p><p>python app.py</p><p>## Example Requests</p><p>What is 245 * 18?</p><p>Summarize this: Python is useful for AI, automation, scripting, and web development.</p><p>What is my public IP?</p><p>## Notes</p><p>- Do not give agents unrestricted tools.<br>- Do not use unsafe `eval()` for arbitrary user input.<br>- Always validate tool names and tool inputs.<br>- Always set timeouts for external API calls.<br>- Add human approval before risky actions such as sending emails, deleting files, or making purchases.</p><hr><h2>6.21 How the Code Works</h2><p>This project has five major parts.</p><h3>Part 1: Tools</h3><p>Each tool is a normal Python function.</p><p>For example:</p><p>calculator(&quot;245 * 18&quot;)</p><p>returns:</p><p>4410</p><p>The agent does not magically know how to calculate.</p><p>It uses your Python calculator tool.</p><h3>Part 2: Tool Registry</h3><p>The registry defines which tools are allowed.</p><p>TOOLS = {<br>&quot;calculator&quot;: Tool(...),<br>&quot;summarize_text&quot;: Tool(...),<br>&quot;get_public_ip&quot;: Tool(...),<br>}</p><p>This is important because the model should not be able to invent dangerous tools.</p><p>If the model tries to choose:</p><p>delete_files</p><p>your app rejects it.</p><h3>Part 3: Tool Router</h3><p>The router asks the LLM to choose one approved tool.</p><p>It returns JSON like this:</p><p>{<br>&quot;tool_name&quot;: &quot;calculator&quot;,<br>&quot;tool_input&quot;: &quot;245 * 18&quot;,<br>&quot;reason&quot;: &quot;The user asked for arithmetic.&quot;<br>}</p><p>Then your code parses and validates the JSON.</p><h3>Part 4: Tool Execution</h3><p>After validation, the app runs:</p><p>tool_result = run_tool(<br>tool_name=decision.tool_name,<br>tool_input=decision.tool_input,<br>)</p><p>The tool result is plain text.</p><h3>Part 5: Final Answer</h3><p>The app sends the tool result back to the LLM and asks for a clear final answer.</p><p>This makes the response friendlier.</p><p>Instead of showing only:</p><p>4410</p><p>the assistant can say:</p><p>245 × 18 = 4410.</p><hr><h2>6.22 Sample User Session</h2><p>A typical session may look like this:</p><p>API Tool-Using Agent<br>========================================<br>Ask for calculations, summaries, or your public IP.<br>Type /exit to quit.<br>========================================</p><p>Request: What is 245 * 18?</p><p>Agent:<br>245 × 18 = 4410.</p><p>Another example:</p><p>Request: Summarize this: Python is popular because it is readable, flexible, and has a large ecosystem for AI, automation, and web development.</p><p>Agent:<br>Here is a concise summary:</p><p>- Python is popular because it is readable and beginner-friendly.<br>- It is flexible enough for AI, automation, and web development.<br>- Its large ecosystem makes it practical for many real-world projects.</p><p>Another example:</p><p>Request: What is my public IP?</p><p>Agent:<br>Your public IP address is: 203.0.113.10</p><p>This is sample output only.</p><p>Do not claim exact output unless you run the code and capture the real result.</p><hr><h2>6.23 Testing the Project</h2><p>Run:</p><p>pytest</p><p>Expected type of result:</p><p>15 passed</p><p>The exact number and timing may vary if you add more tests.</p><p>These tests check:</p><ul><li>Calculator behavior</li><li>Rejection of unsafe expressions</li><li>Tool registry contents</li><li>Disallowed tool handling</li><li>Router prompt creation</li><li>Router JSON parsing</li><li>Unsupported tool rejection</li></ul><p>Most tests do not call the LLM.</p><p>That is intentional.</p><p>A good AI app separates normal Python logic from live model calls.</p><hr><h2>6.24 Live API Testing</h2><p>After adding your API key to .env, run:</p><p>python app.py</p><p>Try:</p><p>What is 88 * 12?</p><p>Summarize this: AI agents combine models, tools, memory, and control logic.</p><p>What is my public IP?</p><p>Also test a confusing or unsafe request:</p><p>Can you delete all my files?</p><p>In this beginner version, no delete tool exists in the registry.</p><p>Since no delete tool exists, the app cannot delete files.</p><p>This is the point of the registry.</p><p>The agent can only use approved tools.</p><hr><h2>6.25 Debugging Tool-Using Agents</h2><p>Tool-using agents can fail in several ways.</p><table><thead><tr><th>Problem</th><th>Likely Cause</th><th>Fix</th></tr></thead><tbody><tr><td>Wrong tool selected</td><td>Router prompt is unclear</td><td>Improve tool descriptions</td></tr><tr><td>JSON parse error</td><td>Model returned extra text</td><td>Strengthen “return only JSON” rule</td></tr><tr><td>Tool input is messy</td><td>Router did not extract input cleanly</td><td>Add examples to router prompt</td></tr><tr><td>Tool fails</td><td>Bad input or external API issue</td><td>Add validation and error handling</td></tr><tr><td>Final answer invents details</td><td>Final prompt too loose</td><td>Tell model to use only tool result</td></tr><tr><td>App is slow</td><td>Too many LLM calls</td><td>Reduce calls or cache</td></tr><tr><td>API call hangs</td><td>No timeout</td><td>Add request timeout</td></tr></tbody></table><p>When debugging, inspect:</p><ul><li>User request</li><li>Router raw response</li><li>Parsed tool decision</li><li>Tool input</li><li>Tool result</li><li>Final answer prompt</li></ul><p>Do not hide the intermediate steps while building.</p><p>Agents become easier to improve when you can see each step.</p><hr><h2>6.26 Why Not Let the Model Run Any Code?</h2><p>A common beginner mistake is to give the model a tool that runs arbitrary Python.</p><p>That is dangerous.</p><p>For example:</p><p>eval(user_input)</p><p>can become unsafe if user input contains malicious code.</p><p>A tool-using agent should not have unlimited power.</p><p>Instead:</p><ul><li>Use narrow tools</li><li>Validate inputs</li><li>Use allowlists</li><li>Add human approval</li><li>Log tool calls</li><li>Avoid destructive actions</li><li>Avoid unrestricted file access</li><li>Avoid arbitrary code execution</li></ul><p>The calculator in this chapter uses Python’s ast module to allow only basic arithmetic.</p><p>That is safer than unrestricted eval().</p><hr><h2>6.27 Tool Safety Rules</h2><p>Use these rules when designing tools.</p><h3>Rule 1: One Tool, One Job</h3><p>A tool should do one clear thing.</p><p>Good:</p><p>calculate arithmetic expression</p><p>Risky:</p><p>run any Python command</p><h3>Rule 2: Validate Inputs</h3><p>Check for empty, long, malformed, or risky input.</p><h3>Rule 3: Limit Permissions</h3><p>A tool should only access what it needs.</p><h3>Rule 4: Add Timeouts</h3><p>External API calls should not wait forever.</p><h3>Rule 5: Log Important Actions</h3><p>Keep a record of what tools were called.</p><h3>Rule 6: Require Human Approval for Risky Actions</h3><p>Human review should be required before:</p><ul><li>Sending emails</li><li>Making purchases</li><li>Deleting data</li><li>Updating records</li><li>Sharing private information</li><li>Triggering financial actions</li></ul><h3>Rule 7: Return Clear Errors</h3><p>A failed tool should not crash the whole app.</p><p>It should return a clear message.</p><hr><h2>6.28 Adding Simple Tool Logs</h2><p>You can add a basic logger.</p><p>Create tool_logger.py:</p><p>&quot;&quot;&quot;<br>Simple tool logging utility.<br>&quot;&quot;&quot;</p><p>from datetime import datetime</p><p>def log_tool_call(tool_name: str, tool_input: str, tool_result: str) -&gt; None:<br>&quot;&quot;&quot;<br>Save a tool call record to a local log file.</p><p>Do not log secrets or sensitive private data in production systems.<br>&quot;&quot;&quot;</p><p>with open(&quot;tool_calls.log&quot;, &quot;a&quot;, encoding=&quot;utf-8&quot;) as file:<br>file.write(f&quot;{datetime.now().isoformat()}\\n&quot;)<br>file.write(f&quot;Tool: {tool_name}\\n&quot;)<br>file.write(f&quot;Input: {tool_input[:500]}\\n&quot;)<br>file.write(f&quot;Result: {tool_result[:500]}\\n&quot;)<br>file.write(&quot;-&quot; * 60 + &quot;\\n&quot;)</p><p>Test result: Python syntax check passed.</p><p>Then update agent.py:</p><p>from tool_logger import log_tool_call</p><p>After running the tool:</p><p>log_tool_call(<br>tool_name=decision.tool_name,<br>tool_input=decision.tool_input,<br>tool_result=tool_result,<br>)</p><p>This is useful for debugging.</p><p>For production systems, logs must be designed carefully to avoid storing sensitive information.</p><hr><h2>6.29 Real-World Use Case</h2><p>Tool-using agents are useful in many real projects.</p><h3>Business Dashboard Assistant</h3><p>User asks:</p><p>What were sales last week?</p><p>Tool calls a database or analytics API.</p><h3>Customer Support Helper</h3><p>User asks:</p><p>Check the refund status for order 1234.</p><p>Tool calls an order system.</p><h3>Research Assistant</h3><p>User asks:</p><p>Find notes about AI guardrails.</p><p>Tool calls semantic search.</p><h3>Developer Assistant</h3><p>User asks:</p><p>Summarize this error log.</p><p>Tool summarizes logs and suggests next steps.</p><h3>Operations Assistant</h3><p>User asks:</p><p>Create a task list from this client email.</p><p>Tool extracts tasks and formats them.</p><p>Tools are where AI assistants start connecting to real workflows.</p><hr><h2>6.30 Monetization and Portfolio Angle</h2><p>You can present this project as:</p><p><strong>API Tool-Using Agent</strong></p><p>Portfolio description:</p><blockquote><p>A Python AI agent that routes user requests to approved tools, including a safe calculator, text summarizer, and public API lookup, then generates a clear final response using an LLM.</p></blockquote><p>Freelance service ideas:</p><ul><li>AI assistant connected to business APIs</li><li>Internal operations helper</li><li>Customer support lookup assistant</li><li>Report generation assistant</li><li>Data query assistant</li><li>Task automation assistant</li><li>RAG assistant with tool calling</li><li>CRM assistant with human approval</li></ul><p>This project is commercially important because clients often want AI systems that do something useful with their existing tools.</p><p>They do not just want a chatbot.</p><p>They want automation.</p><hr><h2>6.31 Common Mistakes</h2><h3>Mistake 1: Giving the Agent Dangerous Tools</h3><p>Do not give a beginner agent tools like:</p><p>delete_file<br>send_payment<br>run_shell_command<br>email_client<br>update_database</p><p>unless you have strict validation and human approval.</p><h3>Mistake 2: Trusting the Router Completely</h3><p>The model can choose the wrong tool.</p><p>Always validate the tool name.</p><h3>Mistake 3: Not Validating Tool Input</h3><p>Bad input creates bad tool results.</p><p>Check for empty input, length, format, and risky patterns.</p><h3>Mistake 4: Using Unrestricted eval</h3><p>Avoid:</p><p>eval(user_input)</p><p>Use safer alternatives.</p><h3>Mistake 5: Hiding Intermediate Steps</h3><p>While developing, inspect:</p><ul><li>Router response</li><li>Parsed decision</li><li>Tool input</li><li>Tool result</li></ul><h3>Mistake 6: Forgetting Timeouts</h3><p>External APIs may be slow or unavailable.</p><p>Use timeouts and handle errors.</p><h3>Mistake 7: Automating Without Approval</h3><p>A tool that sends emails, deletes files, updates records, or makes purchases should require human review.</p><hr><h2>6.32 Best Practices</h2><p>Keep tools narrow.</p><p>Use a tool registry.</p><p>Validate tool names.</p><p>Validate tool inputs.</p><p>Return clear errors.</p><p>Set API timeouts.</p><p>Log tool calls carefully.</p><p>Do not give unrestricted file, shell, or database access.</p><p>Use human approval for risky actions.</p><p>Test tools independently before connecting them to an agent.</p><p>Start with read-only tools before adding write actions.</p><hr><h2>6.33 Mini Challenges</h2><h3>Challenge 1: Add a Word Count Tool</h3><p>Create tools/text_stats.py:</p><pre><code>def word_count(text: str) -&gt; str:\n if not text.strip():\n return &quot;Word count error: text cannot be empty.&quot;</code></pre><pre><code>count = len(text.split())\n return f&quot;Word count: {count}&quot;</code></pre><p>Test result: Python syntax check passed.</p><p>Then add it to tool_registry.py.</p><h3>Challenge 2: Add a Character Count Tool</h3><p>Add:</p><pre><code>def character_count(text: str) -&gt; str:\n return f&quot;Character count: {len(text)}&quot;</code></pre><p>Then update the router prompt so the agent can select it.</p><h3>Challenge 3: Add Tool Call Logging</h3><p>Use the tool_logger.py example and log every tool call.</p><h3>Challenge 4: Add a Human Approval Gate</h3><p>Before running get_public_ip, ask:</p><p>This tool will call an external API. Continue? yes/no</p><p>This introduces human-in-the-loop design.</p><h3>Challenge 5: Add a RAG Search Tool</h3><p>Connect your Chapter 5 semantic search engine as a tool.</p><p>Example tool name:</p><p>search_notes</p><p>Then the agent can answer:</p><p>Find my notes about guardrails.</p><hr><h2>6.34 Chapter Summary</h2><p>In this chapter, you built your first tool-using agent.</p><p>You learned that tools are Python functions exposed to an AI workflow in a controlled way.</p><p>You built:</p><ul><li>A safe calculator tool</li><li>A summarizer tool</li><li>A public API lookup tool</li><li>A tool registry</li><li>A tool router</li><li>A final answer generator</li><li>Tests for tool logic and router parsing</li></ul><p>This is a major step in your AI agent journey.</p><p>Your apps are no longer limited to generating text.</p><p>They can now use approved tools to perform useful actions.</p><p>In the next chapter, you will add another important agent capability:</p><blockquote><p>Memory.</p></blockquote><p>A tool-using agent can act.</p><p>A memory-powered agent can remember.</p><hr><h2>Key Takeaways</h2><ul><li>A tool is a Python function an agent can use.</li><li>Tool calling lets AI apps connect to external systems and actions.</li><li>The model may suggest a tool, but your code must validate it.</li><li>A tool registry prevents the agent from using unapproved tools.</li><li>Tool inputs must be validated.</li><li>External API calls should use timeouts.</li><li>Avoid unrestricted eval() and arbitrary code execution.</li><li>Tool-using agents need logging, safety, and human approval for risky actions.</li><li>This project is a strong portfolio and freelance foundation.</li></ul><hr><h2>Action Checklist</h2><p>Before moving on, confirm:</p><p>[ ] I understand what a tool is.<br>[ ] I understand why tools make AI agents more useful.<br>[ ] I created the Chapter 6 project folder.<br>[ ] I built a safe calculator tool.<br>[ ] I built an API lookup tool.<br>[ ] I built a summarizer tool.<br>[ ] I created a tool registry.<br>[ ] I created a tool router.<br>[ ] I validated tool names before execution.<br>[ ] I ran non-API tests with pytest.<br>[ ] I tested the live app with example requests.<br>[ ] I understand why dangerous tools need human approval.</p><hr><h2>Portfolio Note</h2><p>Add this project to your portfolio as:</p><p><strong>API Tool-Using Agent</strong></p><p>Description:</p><blockquote><p>A Python AI agent that routes user requests to approved tools, including a safe calculator, text summarizer, and public API lookup, then produces a clear final answer using an LLM.</p></blockquote><p>Suggested README tagline:</p><blockquote><p>“A beginner-friendly Python AI agent that can choose tools, call APIs, calculate safely, summarize text, and explain results.”</p></blockquote>",
  "content_hash": "4ef04629d8039ffc9eadc0133db4e8b21646a4c3f0d7474267ef3a1f3aae8e1e",
  "processing_status": "ready"
}
