{
  "id": "chapter-005",
  "title": "Chapter 5: Embeddings, Vector Databases, and Semantic Search",
  "order": 5,
  "content": "<h1>Chapter 5: Embeddings, Vector Databases, and Semantic Search</h1><h2>Opening Hook: Search by Meaning, Not Just Words</h2><p>In Chapter 4, you built your first RAG assistant.</p><p>It could load a PDF, split it into chunks, retrieve relevant chunks, and answer questions using document context.</p><p>That was a major step.</p><p>But the retrieval method was simple.</p><p>It mostly looked for overlapping words.</p><p>That works when the user’s question uses the same words as the document.</p><p>For example:</p><p>Question:<br>What is the refund policy?</p><p>Document chunk:<br>The refund policy allows customers to request a refund within 30 days.</p><p>Keyword retrieval works well here because the word “refund” appears in both places.</p><p>But what if the user asks:</p><p>Can customers get their money back?</p><p>The document may still say:</p><p>The refund policy allows customers to request a refund within 30 days.</p><p>A keyword search may miss the connection because the words are different.</p><p>The meaning is similar, but the wording is not.</p><p>This is where <strong>embeddings</strong> and <strong>semantic search</strong> become useful.</p><p>Semantic search means search by meaning.</p><p>Instead of asking:</p><p>Do these words match?</p><p>semantic search asks:</p><p>Do these ideas mean something similar?</p><p>In this chapter, you will build a personal knowledge search engine.</p><p>It will take notes, split them into chunks, create embeddings, store them in a vector database, and search them by meaning.</p><p>This is the next major step toward stronger RAG systems.</p><hr><h2>What You Will Learn</h2><p>By the end of this chapter, you will understand:</p><ul><li>What embeddings are</li><li>Why embeddings are useful</li><li>What semantic search means</li><li>What vector databases do</li><li>How similarity search works</li><li>How to store text chunks with metadata</li><li>How to build a personal knowledge search engine</li><li>How embeddings improve RAG retrieval</li><li>How to test text processing logic without live API calls</li><li>How this project can become a portfolio or freelance asset</li></ul><hr><h2>Why This Chapter Matters</h2><p>A RAG system is only as good as its retrieval.</p><p>If retrieval finds the wrong chunks, the model may answer poorly.</p><p>If retrieval finds the right chunks, the model has a much better chance of giving a useful answer.</p><p>Chapter 4 used keyword retrieval because it made the RAG pattern easy to understand.</p><p>This chapter upgrades retrieval.</p><p>You will learn the pattern used by many modern document assistants:</p><p>Documents<br>↓<br>Chunks<br>↓<br>Embeddings<br>↓<br>Vector database<br>↓<br>Semantic search<br>↓<br>Relevant context<br>↓<br>LLM answer</p><p>This pattern is used in:</p><ul><li>Internal knowledge bots</li><li>PDF chat assistants</li><li>Customer support assistants</li><li>Research tools</li><li>Developer documentation search</li><li>Study assistants</li><li>Business document Q&amp;A systems</li></ul><p>Embeddings and vector databases are core AI engineering skills.</p><hr><h2>5.1 What Is an Embedding?</h2><p>An embedding is a numeric representation of text.</p><p>For example, the sentence:</p><p>Customers can request a refund within 30 days.</p><p>can be converted into a list of numbers.</p><p>That list might look conceptually like this:</p><p>[0.12, -0.04, 0.88, 0.31, ...]</p><p>The actual list is usually much longer.</p><p>You do not need to understand every number.</p><p>The important idea is this:</p><blockquote><p>Text with similar meaning gets similar numeric representations.</p></blockquote><p>For example:</p><p>Customers can request a refund.</p><p>and:</p><p>Customers can get their money back.</p><p>may produce embeddings that are close to each other.</p><p>That is what makes semantic search possible.</p><hr><h2>5.2 What Is Semantic Search?</h2><p>Semantic search finds results based on meaning.</p><p>Keyword search looks for matching words.</p><p>Semantic search looks for similar ideas.</p><p>Example:</p><p>Query:<br>How can users reset their password?</p><p>Document:<br>Account recovery is available from the login page.</p><p>Keyword search may struggle because “reset password” and “account recovery” are different words.</p><p>Semantic search may understand that these ideas are related.</p><p>This makes it better for natural language questions.</p><hr><h2>5.3 What Is a Vector Database?</h2><p>A vector database stores embeddings and lets you search by similarity.</p><p>A normal database might store:</p><p>name<br>email<br>date<br>price<br>status</p><p>A vector database stores:</p><p>text chunk<br>embedding vector<br>metadata</p><p>When you ask a question, the app creates an embedding for the question.</p><p>Then it searches for text chunks whose embeddings are most similar.</p><p>The flow looks like this:</p><p>User question<br>↓<br>Question embedding<br>↓<br>Vector database search<br>↓<br>Most similar chunks<br>↓<br>LLM answer</p><p>In this chapter, you will use Chroma as a beginner-friendly local vector database.</p><hr><h2>5.4 Similarity Search</h2><p>Similarity search compares vectors.</p><p>The goal is to find which stored vectors are closest to the query vector.</p><p>You do not need to implement advanced vector math yourself in this chapter.</p><p>The vector database handles it.</p><p>At a high level:</p><p>Query: &quot;How do I get a refund?&quot;<br>↓<br>Embedding: [numbers]<br>↓<br>Compare against stored chunk embeddings<br>↓<br>Return closest chunks</p><p>This is what powers semantic retrieval.</p><hr><h2>5.5 Embeddings vs LLMs</h2><p>Embeddings and LLMs are different.</p><table><thead><tr><th>Feature</th><th>Embedding Model</th><th>LLM</th></tr></thead><tbody><tr><td>Main job</td><td>Convert text into vectors</td><td>Generate text</td></tr><tr><td>Output</td><td>Numbers</td><td>Words</td></tr><tr><td>Used for</td><td>Search, clustering, similarity</td><td>Answers, summaries, reasoning</td></tr><tr><td>Example</td><td>Find relevant document chunks</td><td>Explain retrieved chunks</td></tr></tbody></table><p>In a RAG app, you often use both.</p><p>Embedding model → finds relevant chunks<br>LLM → writes the answer</p><hr><h2>5.6 Why This Improves RAG</h2><p>In Chapter 4, retrieval depended on keyword overlap.</p><p>In this chapter, retrieval uses meaning.</p><p>That means the app can find related chunks even when the user uses different words.</p><p>Example:</p><p>User:<br>Can I get my money back?</p><p>Document:<br>Refunds are available within 30 days.</p><p>Semantic search is more likely to connect these ideas.</p><p>This makes the document assistant feel smarter and more natural.</p><hr><h2>5.7 Project: Personal Knowledge Search Engine</h2><h3>What This Project Does</h3><p>This project builds a local semantic search engine for your notes.</p><p>It will:</p><ol><li>Load .txt and .md files from a notes folder</li><li>Split them into chunks</li><li>Create embeddings for each chunk</li><li>Store chunks in a local Chroma database</li><li>Search notes by meaning</li><li>Display matching chunks with source information</li></ol><h3>Who This Helps</h3><p>This project helps:</p><ul><li>Students searching study notes</li><li>Developers searching technical notes</li><li>Freelancers searching project notes</li><li>Writers searching research notes</li><li>Professionals searching personal knowledge bases</li><li>AI builders improving RAG retrieval</li></ul><h3>What You Will Build</h3><p>Create this project structure:</p><p>chapter_05_semantic_search/<br>notes/<br>example_note.md<br>chroma_db/<br>.env.example<br>.gitignore<br>requirements.txt<br>llm_client.py<br>note_loader.py<br>text_utils.py<br>embedding_client.py<br>vector_store.py<br>search_app.py<br>test_note_loader.py<br>test_text_utils.py<br>test_vector_store_helpers.py<br>README.md</p><hr><h2>5.8 Setup</h2><h3>Step 1: Create the Folder</h3><p>mkdir chapter_05_semantic_search<br>cd chapter_05_semantic_search<br>mkdir notes chroma_db</p><h3>Step 2: Create a Virtual Environment</h3><p>python -m venv .venv</p><p>Activate it.</p><p>On macOS or Linux:</p><p>source .venv/bin/activate</p><p>On Windows PowerShell:</p><p>.venv\\Scripts\\Activate.ps1</p><h3>Step 3: Create requirements.txt</h3><p>openai<br>python-dotenv<br>chromadb<br>pytest</p><p>Install dependencies:</p><p>pip install -r requirements.txt</p><h3>Step 4: Create .env.example</h3><p>OPENAI_API_KEY=replace_with_your_api_key<br>OPENAI_EMBEDDING_MODEL=text-embedding-3-small<br>CHROMA_PATH=chroma_db<br>COLLECTION_NAME=personal_notes</p><p>Copy it to .env.</p><p>On macOS or Linux:</p><p>cp .env.example .env</p><p>On Windows PowerShell:</p><p>Copy-Item .env.example .env</p><p>Then edit .env and add your real API key.</p><p>Never publish your real .env file.</p><p>Model names, pricing, and provider behavior may change. Before publishing or deploying, verify current provider documentation.</p><hr><h2>5.9 File: .gitignore</h2><p>Create .gitignore:</p><p>.env<br>.venv/<br>__pycache__/<br>.pytest_cache/<br>*.pyc<br>*.log<br>chroma_db/</p><p>The local vector database is generated data, so it does not need to be committed.</p><hr><h2>5.10 File: notes/example_note.md</h2><p>Create a sample note:</p><p># Agentic AI With Python Notes</p><p>AI agents are software systems that use language models, tools, memory, and workflows to help complete user goals.</p><p>RAG stands for Retrieval-Augmented Generation. It retrieves relevant information before generating an answer.</p><p>Tool calling allows an AI assistant to use approved functions, such as calculators, search tools, or APIs.</p><p>Memory helps an assistant remember useful context, but it must be designed carefully to protect privacy.</p><p>Guardrails are checks and rules that help reduce risky behavior, unsupported claims, and unsafe actions.</p><p>This gives you something to search.</p><hr><h2>5.11 File: llm_client.py</h2><p>This project mostly needs embeddings, but you may later use the same LLM client for answer generation.</p><p>Create llm_client.py:</p><p>&quot;&quot;&quot;<br>Reusable LLM client for Chapter 5.</p><p>This file is included for consistency with later RAG projects.<br>&quot;&quot;&quot;</p><pre><code>import os\nfrom dataclasses import dataclass, field\nfrom dotenv import load_dotenv\nfrom openai import OpenAI</code></pre><p>load_dotenv()</p><p>def get_env_float(name: str, default: float) -&gt; float:<br>&quot;&quot;&quot;Read a float from an environment variable safely.&quot;&quot;&quot;</p><p>raw_value = os.getenv(name)</p><p>if raw_value is None:<br>return default</p><pre><code>try:\n return float(raw_value)\n except ValueError:\n return default</code></pre><p>@dataclass<br>class LLMConfig:<br>&quot;&quot;&quot;Configuration for the LLM client.&quot;&quot;&quot;</p><pre><code>model: str = field(\n default_factory=lambda: os.getenv(&quot;OPENAI_MODEL&quot;, &quot;gpt-4.1-mini&quot;)\n )\n temperature: float = field(\n default_factory=lambda: get_env_float(&quot;OPENAI_TEMPERATURE&quot;, 0.2)\n )</code></pre><p>class LLMClient:<br>&quot;&quot;&quot;Reusable client for text generation.&quot;&quot;&quot;</p><pre><code>def __init__(self, config: LLMConfig | None = None):\n self.config = config or LLMConfig()\n api_key = os.getenv(&quot;OPENAI_API_KEY&quot;)</code></pre><p>if not api_key:<br>raise ValueError(<br>&quot;OPENAI_API_KEY is missing. Add it to your .env file.&quot;<br>)</p><p>self.client = OpenAI(api_key=api_key)</p><p>def complete(self, system: str, user: str) -&gt; str:<br>&quot;&quot;&quot;Send a request to the model and return text output.&quot;&quot;&quot;</p><pre><code>if not system.strip():\n raise ValueError(&quot;System instruction cannot be empty.&quot;)</code></pre><pre><code>if not user.strip():\n raise ValueError(&quot;User request cannot be empty.&quot;)</code></pre><p>model_input = f&quot;&quot;&quot;<br>System instruction:<br>{system.strip()}</p><p>User request:<br>{user.strip()}<br>&quot;&quot;&quot;</p><p>response = self.client.responses.create(<br>model=self.config.model,<br>input=model_input,<br>temperature=self.config.temperature,<br>)</p><p>return response.output_text</p><p>Not executed automatically: requires live API credentials, network access, or external service.</p><hr><h2>5.12 File: note_loader.py</h2><p>This file loads text and Markdown notes.</p><p>Create note_loader.py:</p><p>&quot;&quot;&quot;<br>Note loading utilities for the Chapter 5 semantic search project.<br>&quot;&quot;&quot;</p><pre><code>from dataclasses import dataclass\nfrom pathlib import Path</code></pre><p>SUPPORTED_EXTENSIONS = {&quot;.txt&quot;, &quot;.md&quot;}</p><p>@dataclass<br>class NoteDocument:<br>&quot;&quot;&quot;A loaded note document.&quot;&quot;&quot;</p><p>path: str<br>text: str</p><p>def is_supported_file(path: Path) -&gt; bool:<br>&quot;&quot;&quot;Return True if a file extension is supported.&quot;&quot;&quot;</p><p>return path.suffix.lower() in SUPPORTED_EXTENSIONS</p><p>def load_note_file(path: Path) -&gt; NoteDocument:<br>&quot;&quot;&quot;Load a single note file.&quot;&quot;&quot;</p><pre><code>if not path.exists():\n raise FileNotFoundError(f&quot;File not found: {path}&quot;)</code></pre><pre><code>if not path.is_file():\n raise ValueError(f&quot;Path is not a file: {path}&quot;)</code></pre><p>if not is_supported_file(path):<br>raise ValueError(f&quot;Unsupported file type: {path.suffix}&quot;)</p><p>text = path.read_text(encoding=&quot;utf-8&quot;)</p><p>return NoteDocument(<br>path=str(path),<br>text=text,<br>)</p><p>def load_notes_from_folder(folder: str) -&gt; list[NoteDocument]:<br>&quot;&quot;&quot;Load all supported notes from a folder.&quot;&quot;&quot;</p><p>folder_path = Path(folder)</p><pre><code>if not folder_path.exists():\n raise FileNotFoundError(f&quot;Folder not found: {folder}&quot;)</code></pre><pre><code>if not folder_path.is_dir():\n raise ValueError(f&quot;Path is not a folder: {folder}&quot;)</code></pre><p>documents = []</p><p>for path in sorted(folder_path.rglob(&quot;*&quot;)):<br>if path.is_file() and is_supported_file(path):<br>documents.append(load_note_file(path))</p><p>return documents</p><p>Test result: Python syntax check passed.</p><hr><h2>5.13 File: text_utils.py</h2><p>This file chunks notes and creates chunk records.</p><p>Create text_utils.py:</p><p>&quot;&quot;&quot;<br>Text utilities for Chapter 5 semantic search.<br>&quot;&quot;&quot;</p><p>from dataclasses import dataclass</p><p>@dataclass<br>class TextChunk:<br>&quot;&quot;&quot;A chunk of text with source metadata.&quot;&quot;&quot;</p><p>id: str<br>text: str<br>source: str<br>chunk_index: int</p><p>def clean_text(text: str) -&gt; str:<br>&quot;&quot;&quot;Normalize whitespace.&quot;&quot;&quot;</p><p>return &quot; &quot;.join(text.split())</p><pre><code>def chunk_text(\n text: str,\n chunk_size: int = 800,\n overlap: int = 120,\n) -&gt; list[str]:\n &quot;&quot;&quot;Split text into overlapping chunks.&quot;&quot;&quot;</code></pre><p>if chunk_size &lt;= 0:<br>raise ValueError(&quot;chunk_size must be greater than 0.&quot;)</p><p>if overlap &lt; 0:<br>raise ValueError(&quot;overlap cannot be negative.&quot;)</p><p>if overlap &gt;= chunk_size:<br>raise ValueError(&quot;overlap must be smaller than chunk_size.&quot;)</p><p>cleaned = clean_text(text)</p><p>if not cleaned:<br>return []</p><pre><code>chunks = []\n start = 0</code></pre><pre><code>while start &lt; len(cleaned):\n end = start + chunk_size\n chunk = cleaned[start:end].strip()</code></pre><p>if chunk:<br>chunks.append(chunk)</p><p>start = end - overlap</p><p>return chunks</p><pre><code>def create_chunks_for_document(\n source: str,\n text: str,\n chunk_size: int = 800,\n overlap: int = 120,\n) -&gt; list[TextChunk]:\n &quot;&quot;&quot;Create TextChunk objects for one document.&quot;&quot;&quot;</code></pre><p>raw_chunks = chunk_text(<br>text=text,<br>chunk_size=chunk_size,<br>overlap=overlap,<br>)</p><p>chunks = []</p><p>for index, chunk in enumerate(raw_chunks):<br>chunk_id = f&quot;{source}::chunk_{index}&quot;</p><p>chunks.append(<br>TextChunk(<br>id=chunk_id,<br>text=chunk,<br>source=source,<br>chunk_index=index,<br>)<br>)</p><p>return chunks</p><p>Test result: Python syntax check passed.</p><hr><h2>5.14 File: embedding_client.py</h2><p>This file creates embeddings using an API.</p><p>Create embedding_client.py:</p><p>&quot;&quot;&quot;<br>Embedding client for Chapter 5 semantic search.<br>&quot;&quot;&quot;</p><pre><code>import os\nfrom dataclasses import dataclass, field\nfrom dotenv import load_dotenv\nfrom openai import OpenAI</code></pre><p>load_dotenv()</p><p>@dataclass<br>class EmbeddingConfig:<br>&quot;&quot;&quot;Configuration for embedding generation.&quot;&quot;&quot;</p><p>model: str = field(<br>default_factory=lambda: os.getenv(<br>&quot;OPENAI_EMBEDDING_MODEL&quot;,<br>&quot;text-embedding-3-small&quot;,<br>)<br>)</p><p>class EmbeddingClient:<br>&quot;&quot;&quot;Client for creating text embeddings.&quot;&quot;&quot;</p><pre><code>def __init__(self, config: EmbeddingConfig | None = None):\n self.config = config or EmbeddingConfig()\n api_key = os.getenv(&quot;OPENAI_API_KEY&quot;)</code></pre><p>if not api_key:<br>raise ValueError(<br>&quot;OPENAI_API_KEY is missing. Add it to your .env file.&quot;<br>)</p><p>self.client = OpenAI(api_key=api_key)</p><p>def embed_text(self, text: str) -&gt; list[float]:<br>&quot;&quot;&quot;Create an embedding for one text string.&quot;&quot;&quot;</p><pre><code>if not text.strip():\n raise ValueError(&quot;Text cannot be empty.&quot;)</code></pre><p>response = self.client.embeddings.create(<br>model=self.config.model,<br>input=text,<br>)</p><p>return response.data[0].embedding</p><p>def embed_texts(self, texts: list[str]) -&gt; list[list[float]]:<br>&quot;&quot;&quot;Create embeddings for multiple text strings.&quot;&quot;&quot;</p><p>cleaned = [text for text in texts if text.strip()]</p><p>if not cleaned:<br>raise ValueError(&quot;At least one non-empty text is required.&quot;)</p><p>response = self.client.embeddings.create(<br>model=self.config.model,<br>input=cleaned,<br>)</p><p>return [item.embedding for item in response.data]</p><p>Not executed automatically: requires live API credentials, network access, or external service.</p><hr><h2>5.15 File: vector_store.py</h2><p>This file stores and searches chunks with Chroma.</p><p>Create vector_store.py:</p><p>&quot;&quot;&quot;<br>Vector store utilities using Chroma for Chapter 5.<br>&quot;&quot;&quot;</p><pre><code>import os\nfrom dataclasses import asdict\nfrom dotenv import load_dotenv\nimport chromadb</code></pre><pre><code>from embedding_client import EmbeddingClient\nfrom text_utils import TextChunk</code></pre><p>load_dotenv()</p><p>def get_chroma_path() -&gt; str:<br>&quot;&quot;&quot;Return local Chroma database path.&quot;&quot;&quot;</p><p>return os.getenv(&quot;CHROMA_PATH&quot;, &quot;chroma_db&quot;)</p><p>def get_collection_name() -&gt; str:<br>&quot;&quot;&quot;Return Chroma collection name.&quot;&quot;&quot;</p><p>return os.getenv(&quot;COLLECTION_NAME&quot;, &quot;personal_notes&quot;)</p><pre><code>def create_chroma_client():\n &quot;&quot;&quot;Create a persistent Chroma client.&quot;&quot;&quot;</code></pre><p>return chromadb.PersistentClient(path=get_chroma_path())</p><pre><code>def get_or_create_collection():\n &quot;&quot;&quot;Get or create the Chroma collection.&quot;&quot;&quot;</code></pre><p>client = create_chroma_client()</p><p>return client.get_or_create_collection(name=get_collection_name())</p><p>def chunk_to_metadata(chunk: TextChunk) -&gt; dict:<br>&quot;&quot;&quot;Convert a TextChunk into metadata.&quot;&quot;&quot;</p><p>data = asdict(chunk)</p><pre><code>return {\n &quot;source&quot;: data[&quot;source&quot;],\n &quot;chunk_index&quot;: data[&quot;chunk_index&quot;],\n }</code></pre><pre><code>def add_chunks_to_vector_store(\n chunks: list[TextChunk],\n embedding_client: EmbeddingClient | None = None,\n) -&gt; int:\n &quot;&quot;&quot;\n Add text chunks to the vector store.</code></pre><p>Returns the number of chunks added.<br>&quot;&quot;&quot;</p><p>if not chunks:<br>return 0</p><pre><code>embedder = embedding_client or EmbeddingClient()\n collection = get_or_create_collection()</code></pre><pre><code>texts = [chunk.text for chunk in chunks]\n embeddings = embedder.embed_texts(texts)</code></pre><p>collection.add(<br>ids=[chunk.id for chunk in chunks],<br>documents=texts,<br>embeddings=embeddings,<br>metadatas=[chunk_to_metadata(chunk) for chunk in chunks],<br>)</p><p>return len(chunks)</p><pre><code>def search_vector_store(\n query: str,\n top_k: int = 5,\n embedding_client: EmbeddingClient | None = None,\n) -&gt; list[dict]:\n &quot;&quot;&quot;\n Search the vector store by semantic similarity.\n &quot;&quot;&quot;</code></pre><pre><code>if not query.strip():\n raise ValueError(&quot;Query cannot be empty.&quot;)</code></pre><p>if top_k &lt;= 0:<br>raise ValueError(&quot;top_k must be greater than 0.&quot;)</p><pre><code>embedder = embedding_client or EmbeddingClient()\n collection = get_or_create_collection()</code></pre><p>query_embedding = embedder.embed_text(query)</p><p>results = collection.query(<br>query_embeddings=[query_embedding],<br>n_results=top_k,<br>)</p><p>output = []</p><pre><code>ids = results.get(&quot;ids&quot;, [[]])[0]\n documents = results.get(&quot;documents&quot;, [[]])[0]\n metadatas = results.get(&quot;metadatas&quot;, [[]])[0]\n distances = results.get(&quot;distances&quot;, [[]])[0]</code></pre><p>for index, doc_id in enumerate(ids):<br>output.append(<br>{<br>&quot;id&quot;: doc_id,<br>&quot;text&quot;: documents[index],<br>&quot;metadata&quot;: metadatas[index],<br>&quot;distance&quot;: distances[index] if index &lt; len(distances) else None,<br>}<br>)</p><p>return output</p><p>def format_search_results(results: list[dict]) -&gt; str:<br>&quot;&quot;&quot;Format search results for display.&quot;&quot;&quot;</p><p>if not results:<br>return &quot;No results found.&quot;</p><p>lines = []</p><pre><code>for index, result in enumerate(results, start=1):\n metadata = result.get(&quot;metadata&quot;) or {}\n source = metadata.get(&quot;source&quot;, &quot;unknown source&quot;)\n chunk_index = metadata.get(&quot;chunk_index&quot;, &quot;unknown&quot;)</code></pre><p>lines.append(f&quot;Result {index}&quot;)<br>lines.append(f&quot;Source: {source}&quot;)<br>lines.append(f&quot;Chunk: {chunk_index}&quot;)<br>lines.append(f&quot;Distance: {result.get('distance')}&quot;)<br>lines.append(&quot;&quot;)<br>lines.append(result.get(&quot;text&quot;, &quot;&quot;))<br>lines.append(&quot;-&quot; * 60)</p><p>return &quot;\\n&quot;.join(lines)</p><p>Test result: Python syntax check passed.</p><hr><h2>5.16 File: search_app.py</h2><p>This is the command-line app.</p><p>Create search_app.py:</p><p>&quot;&quot;&quot;<br>Chapter 5 Project: Personal Knowledge Search Engine</p><p>This app indexes notes and searches them by meaning.<br>&quot;&quot;&quot;</p><pre><code>from note_loader import load_notes_from_folder\nfrom text_utils import create_chunks_for_document\nfrom vector_store import (\n add_chunks_to_vector_store,\n search_vector_store,\n format_search_results,\n)</code></pre><pre><code>def index_notes(notes_folder: str = &quot;notes&quot;) -&gt; None:\n &quot;&quot;&quot;Load, chunk, embed, and store notes.&quot;&quot;&quot;</code></pre><p>print(f&quot;\\nLoading notes from: {notes_folder}&quot;)<br>documents = load_notes_from_folder(notes_folder)</p><p>if not documents:<br>print(&quot;No supported notes found.&quot;)<br>return</p><p>all_chunks = []</p><p>for document in documents:<br>chunks = create_chunks_for_document(<br>source=document.path,<br>text=document.text,<br>)<br>all_chunks.extend(chunks)</p><p>print(f&quot;Created {len(all_chunks)} chunk(s).&quot;)<br>print(&quot;Creating embeddings and adding to vector store...&quot;)</p><p>added = add_chunks_to_vector_store(all_chunks)</p><p>print(f&quot;Added {added} chunk(s) to vector store.&quot;)</p><p>def search_loop() -&gt; None:<br>&quot;&quot;&quot;Run interactive semantic search.&quot;&quot;&quot;</p><p>print(&quot;\\nSemantic Search&quot;)<br>print(&quot;=&quot; * 40)<br>print(&quot;Type a question or search query.&quot;)<br>print(&quot;Type /exit to quit.&quot;)<br>print(&quot;=&quot; * 40)</p><p>while True:<br>query = input(&quot;\\nSearch: &quot;).strip()</p><p>if query.lower() in {&quot;/exit&quot;, &quot;exit&quot;, &quot;quit&quot;}:<br>print(&quot;Goodbye.&quot;)<br>break</p><p>if not query:<br>print(&quot;Please enter a search query.&quot;)<br>continue</p><p>try:<br>results = search_vector_store(query=query, top_k=5)<br>print(&quot;\\nResults:&quot;)<br>print(format_search_results(results))<br>except Exception as error:<br>print(f&quot;Search error: {error}&quot;)</p><p>def main() -&gt; None:<br>&quot;&quot;&quot;Run the app.&quot;&quot;&quot;</p><p>print(&quot;\\nPersonal Knowledge Search Engine&quot;)<br>print(&quot;=&quot; * 50)<br>print(&quot;1. Index notes&quot;)<br>print(&quot;2. Search notes&quot;)<br>print(&quot;=&quot; * 50)</p><p>choice = input(&quot;\\nChoose an option: &quot;).strip()</p><p>if choice == &quot;1&quot;:<br>index_notes()<br>elif choice == &quot;2&quot;:<br>search_loop()<br>else:<br>print(&quot;Unknown option.&quot;)</p><p>if __name__ == &quot;__main__&quot;:<br>main()</p><p>Test result: Python syntax check passed.</p><hr><h2>5.17 File: test_note_loader.py</h2><p>Create test_note_loader.py:</p><p>from pathlib import Path</p><p>import pytest</p><p>from note_loader import (<br>is_supported_file,<br>load_note_file,<br>load_notes_from_folder,<br>)</p><pre><code>def test_is_supported_file():\n assert is_supported_file(Path(&quot;note.md&quot;)) is True\n assert is_supported_file(Path(&quot;note.txt&quot;)) is True\n assert is_supported_file(Path(&quot;note.pdf&quot;)) is False</code></pre><pre><code>def test_load_note_file(tmp_path: Path):\n note = tmp_path / &quot;note.md&quot;\n note.write_text(&quot;# Test Note\\nHello world&quot;, encoding=&quot;utf-8&quot;)</code></pre><p>document = load_note_file(note)</p><pre><code>assert document.path == str(note)\n assert &quot;Hello world&quot; in document.text</code></pre><pre><code>def test_load_note_file_missing(tmp_path: Path):\n missing = tmp_path / &quot;missing.md&quot;</code></pre><p>with pytest.raises(FileNotFoundError):<br>load_note_file(missing)</p><pre><code>def test_load_note_file_unsupported(tmp_path: Path):\n file = tmp_path / &quot;note.pdf&quot;\n file.write_text(&quot;fake pdf&quot;, encoding=&quot;utf-8&quot;)</code></pre><p>with pytest.raises(ValueError):<br>load_note_file(file)</p><p>def test_load_notes_from_folder(tmp_path: Path):<br>(tmp_path / &quot;a.md&quot;).write_text(&quot;A note&quot;, encoding=&quot;utf-8&quot;)<br>(tmp_path / &quot;b.txt&quot;).write_text(&quot;B note&quot;, encoding=&quot;utf-8&quot;)<br>(tmp_path / &quot;c.pdf&quot;).write_text(&quot;Unsupported&quot;, encoding=&quot;utf-8&quot;)</p><p>documents = load_notes_from_folder(str(tmp_path))</p><p>assert len(documents) == 2</p><pre><code>def test_load_notes_from_missing_folder():\n with pytest.raises(FileNotFoundError):\n load_notes_from_folder(&quot;missing_folder&quot;)</code></pre><p>Test result: Python syntax check passed.</p><hr><h2>5.18 File: test_text_utils.py</h2><p>Create test_text_utils.py:</p><p>import pytest</p><p>from text_utils import clean_text, chunk_text, create_chunks_for_document</p><pre><code>def test_clean_text():\n assert clean_text(&quot;Hello world\\nagain&quot;) == &quot;Hello world again&quot;</code></pre><pre><code>def test_chunk_text_returns_chunks():\n text = &quot;Python AI agents are useful. &quot; * 100</code></pre><p>chunks = chunk_text(text, chunk_size=100, overlap=20)</p><pre><code>assert len(chunks) &gt; 1\n assert all(len(chunk) &lt;= 100 for chunk in chunks)</code></pre><pre><code>def test_chunk_text_empty():\n assert chunk_text(&quot; &quot;) == []</code></pre><pre><code>def test_chunk_text_invalid_size():\n with pytest.raises(ValueError):\n chunk_text(&quot;hello&quot;, chunk_size=0)</code></pre><pre><code>def test_chunk_text_invalid_overlap():\n with pytest.raises(ValueError):\n chunk_text(&quot;hello&quot;, chunk_size=100, overlap=100)</code></pre><pre><code>def test_create_chunks_for_document():\n text = &quot;Python AI agents are useful. &quot; * 50</code></pre><p>chunks = create_chunks_for_document(<br>source=&quot;notes/test.md&quot;,<br>text=text,<br>chunk_size=100,<br>overlap=20,<br>)</p><pre><code>assert len(chunks) &gt; 1\n assert chunks[0].source == &quot;notes/test.md&quot;\n assert chunks[0].chunk_index == 0\n assert &quot;notes/test.md::chunk_0&quot; == chunks[0].id</code></pre><p>Test result: Python syntax check passed.</p><hr><h2>5.19 File: test_vector_store_helpers.py</h2><p>Create test_vector_store_helpers.py:</p><pre><code>from text_utils import TextChunk\nfrom vector_store import chunk_to_metadata, format_search_results</code></pre><pre><code>def test_chunk_to_metadata():\n chunk = TextChunk(\n id=&quot;note.md::chunk_0&quot;,\n text=&quot;Python AI agents&quot;,\n source=&quot;note.md&quot;,\n chunk_index=0,\n )</code></pre><p>metadata = chunk_to_metadata(chunk)</p><pre><code>assert metadata[&quot;source&quot;] == &quot;note.md&quot;\n assert metadata[&quot;chunk_index&quot;] == 0</code></pre><pre><code>def test_format_search_results_empty():\n assert format_search_results([]) == &quot;No results found.&quot;</code></pre><pre><code>def test_format_search_results_with_result():\n results = [\n {\n &quot;id&quot;: &quot;note.md::chunk_0&quot;,\n &quot;text&quot;: &quot;RAG retrieves context before generation.&quot;,\n &quot;metadata&quot;: {\n &quot;source&quot;: &quot;note.md&quot;,\n &quot;chunk_index&quot;: 0,\n },\n &quot;distance&quot;: 0.12,\n }\n ]</code></pre><p>formatted = format_search_results(results)</p><pre><code>assert &quot;Result 1&quot; in formatted\n assert &quot;note.md&quot; in formatted\n assert &quot;RAG retrieves context&quot; in formatted</code></pre><p>Test result: Python syntax check passed.</p><hr><h2>5.20 File: README.md</h2><p>Create README.md:</p><p>## Project: Personal Knowledge Search Engine</p><p>This project builds a semantic search engine for personal notes using embeddings and a local Chroma vector database.</p><p>## Features</p><p>- Loads `.txt` and `.md` notes<br>- Splits notes into chunks<br>- Creates embeddings<br>- Stores chunks in Chroma<br>- Searches notes by meaning<br>- Displays matching chunks with source metadata<br>- Includes non-API tests for helper logic</p><p>## Setup</p><p>python -m venv .venv</p><p>source .venv/bin/activate</p><p>pip install -r requirements.txt</p><p>cp .env.example .env</p><p>On Windows PowerShell:</p><p>python -m venv .venv</p><p>.venv\\Scripts\\Activate.ps1</p><p>pip install -r requirements.txt</p><p>Copy-Item .env.example .env</p><p>Edit `.env` and add your API key.</p><p>## Run Tests</p><p>pytest</p><p>## Add Notes</p><p>Put `.md` or `.txt` files in:</p><p>notes/</p><p>## Index Notes</p><p>python search_app.py</p><p>Choose option:</p><ol><li>Index notes</li></ol><p>## Search Notes</p><p>python search_app.py</p><p>Choose option:</p><ol><li>Search notes</li></ol><p>Example search:</p><p>How do AI agents use tools?</p><p>## Important Notes</p><p>- This project uses embeddings and Chroma for semantic search.<br>- The local database is stored in `chroma_db/`.<br>- Do not commit `.env`.<br>- Model names, pricing, and embedding behavior may change over time.</p><hr><h2>5.21 How the Code Works</h2><p>This project has five major stages.</p><h3>Stage 1: Load Notes</h3><p>The app loads Markdown and text files from the notes/ folder.</p><p>documents = load_notes_from_folder(&quot;notes&quot;)</p><p>Each document includes:</p><p>path<br>text</p><h3>Stage 2: Chunk Notes</h3><p>Each note is split into chunks.</p><p>chunks = create_chunks_for_document(<br>source=document.path,<br>text=document.text,<br>)</p><p>Each chunk includes:</p><p>id<br>text<br>source<br>chunk_index</p><p>This metadata helps show where search results came from.</p><h3>Stage 3: Create Embeddings</h3><p>The embedding client converts chunk text into numeric vectors.</p><p>embeddings = embedder.embed_texts(texts)</p><p>Each text chunk becomes a vector.</p><h3>Stage 4: Store in Chroma</h3><p>The vector store saves:</p><p>chunk id<br>chunk text<br>embedding<br>metadata</p><p>This makes the chunks searchable by meaning.</p><h3>Stage 5: Search by Meaning</h3><p>When the user enters a query, the app creates a query embedding and searches the vector database.</p><p>results = search_vector_store(query=&quot;How do agents use tools?&quot;)</p><p>The closest chunks are displayed.</p><hr><h2>5.22 Sample User Session</h2><p>First, index notes:</p><p>Personal Knowledge Search Engine<br>==================================================<br>1. Index notes<br>2. Search notes<br>==================================================</p><p>Choose an option: 1</p><p>Loading notes from: notes<br>Created 1 chunk(s).<br>Creating embeddings and adding to vector store...<br>Added 1 chunk(s) to vector store.</p><p>Then search:</p><p>Personal Knowledge Search Engine<br>==================================================<br>1. Index notes<br>2. Search notes<br>==================================================</p><p>Choose an option: 2</p><p>Semantic Search<br>========================================<br>Type a question or search query.<br>Type /exit to quit.<br>========================================</p><p>Search: How do AI agents use functions?</p><p>Results:<br>Result 1<br>Source: notes/example_note.md<br>Chunk: 0<br>Distance: 0.24</p><p>AI agents are software systems that use language models, tools, memory, and workflows...<br>------------------------------------------------------------</p><p>This is sample output only.</p><p>Do not claim exact output unless you run the code and capture the real result.</p><hr><h2>5.23 Testing the Project</h2><p>Run:</p><p>pytest</p><p>Expected type of result:</p><p>15 passed</p><p>The exact number and timing may vary if you add more tests.</p><p>These tests check:</p><ul><li>Note loading</li><li>Supported file detection</li><li>Chunking</li><li>Chunk metadata</li><li>Search result formatting</li></ul><p>The tests do not call the live embedding API.</p><p>That is intentional.</p><p>Embedding calls can cost money and require network access.</p><p>Test normal Python logic separately.</p><hr><h2>5.24 Live Embedding Test</h2><p>After adding your API key to .env, run:</p><p>python search_app.py</p><p>Choose:</p><p>1. Index notes</p><p>Then run the app again and choose:</p><p>2. Search notes</p><p>Try:</p><p>How do agents remember things?</p><p>If your sample note includes memory-related text, the search should return the chunk about memory.</p><hr><h2>5.25 Debugging Semantic Search</h2><p>Semantic search can fail too.</p><p>Common problems:</p><table><thead><tr><th>Problem</th><th>Likely Cause</th><th>Fix</th></tr></thead><tbody><tr><td>No results</td><td>Notes not indexed</td><td>Run indexing first</td></tr><tr><td>Old results</td><td>Database contains old chunks</td><td>Delete chroma_db/ and reindex</td></tr><tr><td>Bad results</td><td>Poor chunks</td><td>Adjust chunk size</td></tr><tr><td>API error</td><td>Missing key</td><td>Check .env</td></tr><tr><td>Duplicate results</td><td>Indexed same notes multiple times</td><td>Clear database before reindexing</td></tr><tr><td>Slow indexing</td><td>Large notes</td><td>Index fewer files first</td></tr><tr><td>Poor relevance</td><td>Query too vague</td><td>Ask more specific question</td></tr></tbody></table><p>A simple reset is:</p><p>rm -rf chroma_db</p><p>Then reindex.</p><p>On Windows PowerShell:</p><p>Remove-Item -Recurse -Force chroma_db</p><hr><h2>5.26 Embeddings and Metadata</h2><p>Embeddings help find meaning.</p><p>Metadata helps explain results.</p><p>A chunk without metadata is harder to trust.</p><p>Good metadata might include:</p><p>source file<br>page number<br>chunk index<br>document title<br>created date<br>document type</p><p>This chapter stores:</p><p>source<br>chunk_index</p><p>That is enough for a beginner project.</p><p>In stronger apps, add more metadata.</p><hr><h2>5.27 How This Improves Chapter 4</h2><p>Chapter 4 answered questions about PDFs using keyword retrieval.</p><p>Chapter 5 adds semantic search.</p><p>The next improvement is combining them:</p><p>PDF<br>↓<br>Extract text<br>↓<br>Chunk text<br>↓<br>Embed chunks<br>↓<br>Store in vector database<br>↓<br>Search by meaning<br>↓<br>Answer using retrieved context</p><p>That is a stronger RAG system.</p><p>You can upgrade Chapter 4 by replacing the keyword retriever with the vector store from this chapter.</p><hr><h2>5.28 Real-World Use Case</h2><p>Semantic search is useful anywhere people have lots of text.</p><h3>Students</h3><p>Search class notes by meaning.</p><p>What did I write about recursion?</p><h3>Developers</h3><p>Search technical notes.</p><p>How did I configure authentication?</p><h3>Freelancers</h3><p>Search client notes.</p><p>Which client asked for a chatbot?</p><h3>Writers</h3><p>Search research notes.</p><p>What notes mention AI safety?</p><h3>Businesses</h3><p>Search internal documentation.</p><p>How do we handle refunds?</p><p>This is why embeddings are so valuable.</p><p>They help AI apps find relevant knowledge.</p><hr><h2>5.29 Monetization and Portfolio Angle</h2><p>You can present this project as:</p><p><strong>Personal Knowledge Search Engine</strong></p><p>Portfolio description:</p><blockquote><p>A Python semantic search application that loads Markdown and text notes, chunks them, creates embeddings, stores them in a local Chroma vector database, and retrieves relevant notes by meaning.</p></blockquote><p>Freelance service ideas:</p><ul><li>Internal knowledge search tool</li><li>Team notes search engine</li><li>AI-powered documentation search</li><li>Course notes search app</li><li>Client knowledge-base search</li><li>Research library assistant</li><li>Personal productivity knowledge tool</li></ul><p>This project is commercially strong because many individuals and businesses have documents they cannot easily search.</p><p>Semantic search solves that problem.</p><hr><h2>5.30 Common Mistakes</h2><h3>Mistake 1: Confusing Embeddings with Chat Responses</h3><p>Embeddings return numbers.</p><p>LLMs return text.</p><p>Use embeddings for search.</p><p>Use LLMs for answers.</p><h3>Mistake 2: Forgetting to Store Metadata</h3><p>Without metadata, users cannot tell where results came from.</p><p>Always store source information.</p><h3>Mistake 3: Reindexing Without Clearing Duplicates</h3><p>If you add the same chunks repeatedly, results may duplicate.</p><p>Clear or update the database carefully.</p><h3>Mistake 4: Testing Only One Query</h3><p>Try different wording.</p><p>Semantic search should handle related meaning, not just exact words.</p><h3>Mistake 5: Ignoring Chunk Quality</h3><p>Bad chunks lead to bad search.</p><p>Chunking still matters.</p><h3>Mistake 6: Sending Secrets in Notes</h3><p>Do not index API keys, passwords, private tokens, or confidential data unless you have proper security and permission.</p><h3>Mistake 7: Treating Distance Scores as Universal</h3><p>Vector database distance scores may vary by system and configuration.</p><p>Use them as relative signals, not absolute truth.</p><hr><h2>5.31 Best Practices</h2><p>Chunk text before embedding.</p><p>Store source metadata.</p><p>Use meaningful collection names.</p><p>Keep local databases out of Git.</p><p>Clear and reindex when changing chunking strategy.</p><p>Test multiple query styles.</p><p>Avoid indexing secrets.</p><p>Use embeddings for retrieval and LLMs for generation.</p><p>Inspect search results before building answers.</p><p>Document limitations in README.</p><hr><h2>5.32 Mini Challenges</h2><h3>Challenge 1: Add More Notes</h3><p>Add at least five Markdown notes to the notes/ folder and index them.</p><h3>Challenge 2: Add Source Filtering</h3><p>Let the user search only one source file.</p><p>Example:</p><p>Search only notes/python.md</p><h3>Challenge 3: Add a Clear Database Option</h3><p>Add a menu option:</p><p>3. Reset vector database</p><h3>Challenge 4: Add RAG Answering</h3><p>After search results are returned, pass them to an LLM and ask for an answer.</p><p>Use the Chapter 4 RAG prompt pattern.</p><h3>Challenge 5: Add File Type Labels</h3><p>Add metadata:</p><p>file_type: md or txt</p><h3>Challenge 6: Add a Search Report</h3><p>Save search query and results to:</p><p>outputs/search_report.md</p><hr><h2>5.33 Chapter Summary</h2><p>In this chapter, you learned how embeddings, vector databases, and semantic search work.</p><p>You built a personal knowledge search engine that:</p><ul><li>Loads notes</li><li>Splits them into chunks</li><li>Creates embeddings</li><li>Stores them in Chroma</li><li>Searches by meaning</li><li>Displays source metadata</li></ul><p>This project upgrades your RAG foundation.</p><p>In Chapter 4, your assistant could search by keyword.</p><p>Now, your assistant can search by meaning.</p><p>In the next chapter, you will add another major agent capability:</p><blockquote><p>Tools.</p></blockquote><p>A search-powered assistant can find information.</p><p>A tool-using agent can take action.</p><hr><h2>Key Takeaways</h2><ul><li>Embeddings convert text into numeric vectors.</li><li>Similar meanings produce similar vectors.</li><li>Semantic search finds results by meaning, not only keywords.</li><li>Vector databases store embeddings and support similarity search.</li><li>Metadata helps users trust and inspect search results.</li><li>Embeddings are usually used for retrieval, while LLMs generate answers.</li><li>Better retrieval improves RAG quality.</li><li>Semantic search is a strong portfolio and freelance skill.</li></ul><hr><h2>Action Checklist</h2><p>Before moving on, confirm:</p><p>[ ] I understand what embeddings are.<br>[ ] I understand semantic search.<br>[ ] I understand what a vector database does.<br>[ ] I created the Chapter 5 project folder.<br>[ ] I created a virtual environment.<br>[ ] I added requirements.txt.<br>[ ] I created .env.example.<br>[ ] I created .gitignore.<br>[ ] I created note_loader.py.<br>[ ] I created text_utils.py.<br>[ ] I created embedding_client.py.<br>[ ] I created vector_store.py.<br>[ ] I created search_app.py.<br>[ ] I added a sample note.<br>[ ] I ran non-API tests with pytest.<br>[ ] I indexed notes with a live API key.<br>[ ] I searched notes by meaning.<br>[ ] I understand how this improves RAG.</p><hr><h2>Portfolio Note</h2><p>Add this project to your portfolio as:</p><p><strong>Personal Knowledge Search Engine</strong></p><p>Description:</p><blockquote><p>A Python semantic search app that loads personal notes, chunks text, creates embeddings, stores them in a local Chroma vector database, and retrieves relevant knowledge by meaning.</p></blockquote><p>Suggested README tagline:</p><blockquote><p>“Search your notes by meaning using Python, embeddings, and a local vector database.”</p></blockquote>",
  "content_hash": "b8ff11d89715ebed26b3d1173055263a91cb568aa30f60e8618350e313e57a4b",
  "processing_status": "ready"
}
