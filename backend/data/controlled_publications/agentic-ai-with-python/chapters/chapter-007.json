{
  "id": "chapter-007",
  "title": "Chapter 7: Memory — Making AI Assistants Remember Context",
  "order": 7,
  "content": "<h1>Chapter 7: Memory — Making AI Assistants Remember Context</h1><h2>Opening Hook: The Difference Between a Chatbot and a Helpful Assistant</h2><p>A chatbot answers what you ask right now.</p><p>A useful assistant remembers what matters.</p><p>Imagine asking:</p><p>Explain RAG.</p><p>A normal chatbot gives a general explanation.</p><p>But a memory-powered assistant can answer differently if it remembers:</p><p>The user is a beginner Python learner.<br>The user prefers short examples.<br>The user is building a portfolio project.<br>The user is interested in freelancing.</p><p>Now the answer can be more helpful:</p><p>RAG is a way to let your Python AI app answer using your own documents.<br>For your portfolio, you could build a PDF chatbot that retrieves relevant chunks before answering.</p><p>That is the value of memory.</p><p>Memory helps an AI assistant become more personal, more useful, and more consistent.</p><p>But memory also creates risk.</p><p>If an assistant remembers everything, it may store secrets, private data, or sensitive information the user never wanted saved.</p><p>So the goal is not:</p><p>Remember everything.</p><p>The goal is:</p><p>Remember useful, user-approved information safely.</p><p>In this chapter, you will build a personal study or work assistant with memory.</p><p>It will let the user explicitly save memories with a /remember command, view saved memories, delete memories, clear memory, and use memory as context in future responses.</p><p>This chapter teaches one of the most important design rules for practical AI agents:</p><blockquote><p>Memory should be useful, visible, editable, and user-controlled.</p></blockquote><hr><h2>What You Will Learn</h2><p>By the end of this chapter, you will understand:</p><ul><li>What memory means in AI assistants</li><li>The difference between short-term and long-term memory</li><li>Why memory improves personalization</li><li>Why memory creates privacy risks</li><li>How to store user-approved memory locally</li><li>How to inspect and delete memory</li><li>How to include memory in prompts safely</li><li>How to block obvious secrets from being saved</li><li>How to build a personal study or work assistant with memory</li><li>How to test memory logic without live API calls</li><li>How memory connects to future workflow and capstone projects</li></ul><hr><h2>Why This Chapter Matters</h2><p>In earlier chapters, your assistant could answer, summarize, retrieve, search, and use tools.</p><p>But each request was mostly isolated.</p><p>A memory-powered assistant can carry useful context across interactions.</p><p>For example:</p><p>User:<br>/remember I am preparing for Python interviews.</p><p>Later:<br>Explain decorators.</p><p>The assistant can now respond with interview preparation in mind.</p><p>Another example:</p><p>User:<br>/remember I prefer concise bullet-point explanations.</p><p>Later:<br>Explain vector databases.</p><p>The assistant can keep the answer short and structured.</p><p>Memory is powerful because it improves relevance.</p><p>But memory must be handled carefully.</p><p>A poorly designed memory system can:</p><ul><li>Store secrets</li><li>Store private information</li><li>Remember incorrect facts</li><li>Use irrelevant memory</li><li>Expose one user’s information to another user</li><li>Make the assistant feel creepy</li><li>Make deletion difficult</li></ul><p>This chapter uses the safest beginner pattern:</p><p>The assistant remembers only when the user explicitly says /remember.<br>The user can inspect memory.<br>The user can delete memory.<br>The user can clear all memory.</p><p>That is simple, practical, and respectful.</p><hr><h2>7.1 What Is Memory in an AI Assistant?</h2><p>Memory is stored information that the assistant can use later.</p><p>In a Python AI app, memory might be stored in:</p><ul><li>A JSON file</li><li>A SQLite database</li><li>A vector database</li><li>A cloud database</li><li>A user profile table</li><li>A conversation history store</li></ul><p>For this chapter, you will use a local JSON file.</p><p>That keeps the project beginner-friendly.</p><p>A memory item might look like this:</p><p>{<br>&quot;id&quot;: 1,<br>&quot;text&quot;: &quot;The user prefers concise explanations.&quot;,<br>&quot;tags&quot;: [&quot;preference&quot;],<br>&quot;created_at&quot;: &quot;2026-05-23T10:30:00&quot;<br>}</p><p>When the user asks a question, the assistant can include relevant memory in the prompt.</p><hr><h2>7.2 Short-Term Memory vs Long-Term Memory</h2><p>There are two common types of memory.</p><h3>Short-Term Memory</h3><p>Short-term memory usually means recent conversation context.</p><p>Example:</p><p>User: I am building a PDF chatbot.<br>Assistant: Great.<br>User: How should I test it?</p><p>The assistant understands that “it” means the PDF chatbot.</p><p>Short-term memory is often stored only during the current session.</p><h3>Long-Term Memory</h3><p>Long-term memory persists across sessions.</p><p>Example:</p><p>The user is building Python AI portfolio projects.<br>The user prefers beginner-friendly explanations.<br>The user is interested in freelancing.</p><p>Long-term memory can be useful, but it must be controlled carefully.</p><p>This chapter focuses on simple long-term memory saved to a JSON file.</p><hr><h2>7.3 What Should an Assistant Remember?</h2><p>Good memory examples:</p><p>The user prefers concise answers.<br>The user is learning Python.<br>The user is building a RAG app.<br>The user wants portfolio project advice.<br>The user prefers examples using simple code.</p><p>Bad memory examples:</p><p>The user's API key is sk-...<br>The user's password is ...<br>The user's private token is ...<br>The user's bank details are ...<br>The user's private medical information is ...</p><p>Do not store secrets.</p><p>Do not store sensitive information unless your app has a real privacy design and the user clearly understands what is being stored.</p><p>For beginner projects, keep memory simple and low-risk.</p><hr><h2>7.4 User-Controlled Memory</h2><p>A safe beginner memory system should support:</p><p>/remember [text]<br>/memory<br>/forget [id]<br>/clear_memory</p><p>This gives the user control.</p><p>The assistant should not silently save everything.</p><p>Silent memory can feel invasive.</p><p>Explicit memory feels respectful.</p><p>The user says what should be remembered.</p><p>The user can review it.</p><p>The user can delete it.</p><p>That is the design you will build.</p><hr><h2>7.5 Project: Personal Study or Work Assistant with Memory</h2><h3>What This Project Does</h3><p>This project builds a command-line assistant with memory.</p><p>It supports:</p><p>/ask [question]<br>/remember [memory]<br>/memory<br>/forget [id]<br>/clear_memory<br>/help<br>/exit</p><p>Examples:</p><p>/remember I prefer concise bullet-point explanations.</p><p>/ask Explain RAG.</p><p>/memory</p><p>/forget 1</p><p>The assistant uses saved memory as context when answering questions.</p><h3>Who This Helps</h3><p>This project helps:</p><ul><li>Students</li><li>Python learners</li><li>Developers</li><li>Freelancers</li><li>Professionals</li><li>Productivity tool builders</li><li>Anyone building more personal AI assistants</li></ul><h3>What You Will Build</h3><p>Create this project structure:</p><p>chapter_07_memory_assistant/<br>.env.example<br>.gitignore<br>requirements.txt<br>llm_client.py<br>memory_store.py<br>prompts.py<br>commands.py<br>assistant.py<br>app.py<br>test_memory_store.py<br>test_commands.py<br>test_assistant.py<br>README.md</p><hr><h2>7.6 Setup</h2><h3>Step 1: Create the Folder</h3><p>mkdir chapter_07_memory_assistant<br>cd chapter_07_memory_assistant</p><h3>Step 2: Create a Virtual Environment</h3><p>python -m venv .venv</p><p>Activate it.</p><p>On macOS or Linux:</p><p>source .venv/bin/activate</p><p>On Windows PowerShell:</p><p>.venv\\Scripts\\Activate.ps1</p><h3>Step 3: Create requirements.txt</h3><p>openai<br>python-dotenv<br>pytest</p><p>Install dependencies:</p><p>pip install -r requirements.txt</p><h3>Step 4: Create .env.example</h3><p>OPENAI_API_KEY=replace_with_your_api_key<br>OPENAI_MODEL=gpt-4.1-mini<br>OPENAI_TEMPERATURE=0.2<br>MEMORY_FILE=memory.json</p><p>Copy it to .env.</p><p>On macOS or Linux:</p><p>cp .env.example .env</p><p>On Windows PowerShell:</p><p>Copy-Item .env.example .env</p><p>Then edit .env and add your real API key.</p><p>Never publish your real .env file.</p><hr><h2>7.7 File: .gitignore</h2><p>Create .gitignore:</p><p>.env<br>.venv/<br>__pycache__/<br>.pytest_cache/<br>*.pyc<br>*.log<br>memory.json</p><p>The memory file may contain user preferences or private notes, so do not commit it.</p><hr><h2>7.8 File: llm_client.py</h2><p>Create llm_client.py:</p><p>&quot;&quot;&quot;<br>Chapter 7: Reusable LLM Client<br>&quot;&quot;&quot;</p><pre><code>import os\nfrom dataclasses import dataclass, field\nfrom dotenv import load_dotenv\nfrom openai import OpenAI</code></pre><p>load_dotenv()</p><p>def get_env_float(name: str, default: float) -&gt; float:<br>&quot;&quot;&quot;Read a float from environment variables safely.&quot;&quot;&quot;</p><p>raw_value = os.getenv(name)</p><p>if raw_value is None:<br>return default</p><pre><code>try:\n return float(raw_value)\n except ValueError:\n return default</code></pre><p>@dataclass<br>class LLMConfig:<br>&quot;&quot;&quot;Configuration for the LLM client.&quot;&quot;&quot;</p><pre><code>model: str = field(\n default_factory=lambda: os.getenv(&quot;OPENAI_MODEL&quot;, &quot;gpt-4.1-mini&quot;)\n )\n temperature: float = field(\n default_factory=lambda: get_env_float(&quot;OPENAI_TEMPERATURE&quot;, 0.2)\n )</code></pre><p>class LLMClient:<br>&quot;&quot;&quot;Small reusable client for sending prompts to an LLM.&quot;&quot;&quot;</p><pre><code>def __init__(self, config: LLMConfig | None = None):\n self.config = config or LLMConfig()\n api_key = os.getenv(&quot;OPENAI_API_KEY&quot;)</code></pre><p>if not api_key:<br>raise ValueError(<br>&quot;OPENAI_API_KEY is missing. Add it to your .env file.&quot;<br>)</p><p>self.client = OpenAI(api_key=api_key)</p><p>def complete(self, system: str, user: str) -&gt; str:<br>&quot;&quot;&quot;Send a request to the model and return text output.&quot;&quot;&quot;</p><pre><code>if not system.strip():\n raise ValueError(&quot;System instruction cannot be empty.&quot;)</code></pre><pre><code>if not user.strip():\n raise ValueError(&quot;User request cannot be empty.&quot;)</code></pre><p>model_input = f&quot;&quot;&quot;<br>System instruction:<br>{system.strip()}</p><p>User request:<br>{user.strip()}<br>&quot;&quot;&quot;</p><p>response = self.client.responses.create(<br>model=self.config.model,<br>input=model_input,<br>temperature=self.config.temperature,<br>)</p><p>return response.output_text</p><p>class MockLLMClient:<br>&quot;&quot;&quot;Mock client for tests.&quot;&quot;&quot;</p><pre><code>def __init__(self, response_text: str = &quot;Mock response&quot;):\n self.response_text = response_text\n self.calls: list[dict[str, str]] = []</code></pre><p>def complete(self, system: str, user: str) -&gt; str:<br>&quot;&quot;&quot;Return fixed output and record the call.&quot;&quot;&quot;</p><pre><code>if not system.strip():\n raise ValueError(&quot;System instruction cannot be empty.&quot;)</code></pre><pre><code>if not user.strip():\n raise ValueError(&quot;User request cannot be empty.&quot;)</code></pre><p>self.calls.append(<br>{<br>&quot;system&quot;: system,<br>&quot;user&quot;: user,<br>}<br>)</p><p>return self.response_text</p><p>Not executed automatically: requires live API credentials, network access, or external service.</p><hr><h2>7.9 File: memory_store.py</h2><p>This file stores and manages memory.</p><p>Create memory_store.py:</p><p>&quot;&quot;&quot;<br>Local JSON memory store for the Chapter 7 memory assistant.</p><p>This beginner-friendly memory store supports:<br>- Add memory<br>- List memory<br>- Delete memory by ID<br>- Clear all memory</p><p>Do not store secrets, passwords, API keys, or sensitive private data.<br>&quot;&quot;&quot;</p><pre><code>import json\nimport os\nimport re\nfrom dataclasses import dataclass, asdict\nfrom datetime import datetime\nfrom pathlib import Path\nfrom dotenv import load_dotenv</code></pre><p>load_dotenv()</p><p>def get_memory_path() -&gt; Path:<br>&quot;&quot;&quot;Return the configured memory file path.&quot;&quot;&quot;</p><p>return Path(os.getenv(&quot;MEMORY_FILE&quot;, &quot;memory.json&quot;))</p><p>@dataclass<br>class MemoryItem:<br>&quot;&quot;&quot;One saved memory item.&quot;&quot;&quot;</p><p>id: int<br>text: str<br>created_at: str</p><p>def default_memory_data() -&gt; dict:<br>&quot;&quot;&quot;Return default memory file structure.&quot;&quot;&quot;</p><pre><code>return {\n &quot;next_id&quot;: 1,\n &quot;items&quot;: [],\n }</code></pre><p>SECRET_PATTERNS = [<br>r&quot;sk-[A-Za-z0-9_\\-]{10,}&quot;,<br>r&quot;api[_-]?key\\s*[:=]\\s*[A-Za-z0-9_\\-]{10,}&quot;,<br>r&quot;password\\s*[:=]\\s*\\S+&quot;,<br>r&quot;token\\s*[:=]\\s*[A-Za-z0-9_\\-\\.]{10,}&quot;,<br>]</p><p>def looks_like_secret(text: str) -&gt; bool:<br>&quot;&quot;&quot;<br>Basic heuristic for detecting possible secrets.</p><p>This is not a complete secret scanner, but it catches common beginner mistakes.<br>&quot;&quot;&quot;</p><p>lowered = text.lower()</p><p>secret_keywords = [<br>&quot;api key&quot;,<br>&quot;password&quot;,<br>&quot;private key&quot;,<br>&quot;secret key&quot;,<br>&quot;access token&quot;,<br>&quot;bearer token&quot;,<br>]</p><p>if any(keyword in lowered for keyword in secret_keywords):<br>return True</p><p>return any(re.search(pattern, text, re.IGNORECASE) for pattern in SECRET_PATTERNS)</p><pre><code>def load_memory_data(path: Path | None = None) -&gt; dict:\n &quot;&quot;&quot;Load memory data from JSON.&quot;&quot;&quot;</code></pre><p>memory_path = path or get_memory_path()</p><pre><code>if not memory_path.exists():\n return default_memory_data()</code></pre><pre><code>try:\n data = json.loads(memory_path.read_text(encoding=&quot;utf-8&quot;))\n except json.JSONDecodeError:\n return default_memory_data()</code></pre><p>if not isinstance(data, dict):<br>return default_memory_data()</p><p>if &quot;next_id&quot; not in data or &quot;items&quot; not in data:<br>return default_memory_data()</p><p>return data</p><pre><code>def save_memory_data(data: dict, path: Path | None = None) -&gt; None:\n &quot;&quot;&quot;Save memory data to JSON.&quot;&quot;&quot;</code></pre><p>memory_path = path or get_memory_path()<br>memory_path.write_text(json.dumps(data, indent=2), encoding=&quot;utf-8&quot;)</p><pre><code>def add_memory(text: str, path: Path | None = None) -&gt; MemoryItem:\n &quot;&quot;&quot;Add a memory item.&quot;&quot;&quot;</code></pre><p>cleaned = text.strip()</p><p>if not cleaned:<br>raise ValueError(&quot;Memory cannot be empty.&quot;)</p><p>if len(cleaned) &gt; 500:<br>raise ValueError(&quot;Memory is too long. Limit: 500 characters.&quot;)</p><p>if looks_like_secret(cleaned):<br>raise ValueError(<br>&quot;This memory appears to contain a secret or sensitive credential. &quot;<br>&quot;Do not store API keys, passwords, or tokens.&quot;<br>)</p><pre><code>data = load_memory_data(path)\n memory_id = int(data[&quot;next_id&quot;])</code></pre><p>item = MemoryItem(<br>id=memory_id,<br>text=cleaned,<br>created_at=datetime.now().isoformat(timespec=&quot;seconds&quot;),<br>)</p><p>data[&quot;items&quot;].append(asdict(item))<br>data[&quot;next_id&quot;] = memory_id + 1</p><p>save_memory_data(data, path)</p><p>return item</p><pre><code>def list_memory(path: Path | None = None) -&gt; list[MemoryItem]:\n &quot;&quot;&quot;List all saved memories.&quot;&quot;&quot;</code></pre><pre><code>data = load_memory_data(path)\n items = []</code></pre><p>for raw_item in data.get(&quot;items&quot;, []):<br>try:<br>items.append(<br>MemoryItem(<br>id=int(raw_item[&quot;id&quot;]),<br>text=str(raw_item[&quot;text&quot;]),<br>created_at=str(raw_item[&quot;created_at&quot;]),<br>)<br>)<br>except (KeyError, TypeError, ValueError):<br>continue</p><p>return items</p><pre><code>def delete_memory(memory_id: int, path: Path | None = None) -&gt; bool:\n &quot;&quot;&quot;Delete memory by ID. Return True if deleted.&quot;&quot;&quot;</code></pre><pre><code>data = load_memory_data(path)\n original_count = len(data[&quot;items&quot;])</code></pre><p>data[&quot;items&quot;] = [<br>item for item in data[&quot;items&quot;]<br>if int(item.get(&quot;id&quot;, -1)) != memory_id<br>]</p><p>deleted = len(data[&quot;items&quot;]) &lt; original_count</p><p>if deleted:<br>save_memory_data(data, path)</p><p>return deleted</p><pre><code>def clear_memory(path: Path | None = None) -&gt; None:\n &quot;&quot;&quot;Clear all memory.&quot;&quot;&quot;</code></pre><p>save_memory_data(default_memory_data(), path)</p><pre><code>def format_memory_for_display(path: Path | None = None) -&gt; str:\n &quot;&quot;&quot;Format memory for terminal display.&quot;&quot;&quot;</code></pre><p>items = list_memory(path)</p><p>if not items:<br>return &quot;No saved memory.&quot;</p><p>lines = []</p><p>for item in items:<br>lines.append(f&quot;{item.id}. {item.text}&quot;)<br>lines.append(f&quot; saved: {item.created_at}&quot;)</p><p>return &quot;\\n&quot;.join(lines)</p><pre><code>def format_memory_for_prompt(path: Path | None = None) -&gt; str:\n &quot;&quot;&quot;Format memory for inclusion in an LLM prompt.&quot;&quot;&quot;</code></pre><p>items = list_memory(path)</p><p>if not items:<br>return &quot;No saved memory.&quot;</p><p>lines = []</p><p>for item in items:<br>lines.append(f&quot;- {item.text}&quot;)</p><p>return &quot;\\n&quot;.join(lines)</p><p>Test result: Python syntax check passed.</p><hr><h2>7.10 File: prompts.py</h2><p>Create prompts.py:</p><p>&quot;&quot;&quot;<br>Prompt templates for the Chapter 7 memory assistant.<br>&quot;&quot;&quot;</p><p>MEMORY_ASSISTANT_SYSTEM_PROMPT = &quot;&quot;&quot;<br>You are a helpful personal study and work assistant.</p><p>You may receive saved memory about the user's preferences, goals, or context.</p><p>Rules:<br>- Use saved memory only when it is relevant.<br>- Do not mention memory unnecessarily.<br>- Do not invent facts about the user.<br>- Do not reveal sensitive information.<br>- Be clear, practical, and encouraging.<br>- If memory conflicts with the current request, prioritize the current request.<br>&quot;&quot;&quot;</p><p>def build_memory_prompt(user_request: str, memory_context: str) -&gt; str:<br>&quot;&quot;&quot;Build prompt that includes saved memory.&quot;&quot;&quot;</p><p>return f&quot;&quot;&quot;<br>Saved memory:<br>{memory_context}</p><p>Current user request:<br>{user_request}</p><p>Respond helpfully.</p><p>If the saved memory is relevant, use it to personalize the answer.<br>If the saved memory is not relevant, ignore it.<br>Do not claim to remember anything that is not listed in saved memory.<br>&quot;&quot;&quot;</p><p>Test result: Python syntax check passed.</p><hr><h2>7.11 File: commands.py</h2><p>Create commands.py:</p><p>&quot;&quot;&quot;<br>Command parsing for the Chapter 7 memory assistant.<br>&quot;&quot;&quot;</p><p>from dataclasses import dataclass</p><p>SUPPORTED_COMMANDS = {<br>&quot;/help&quot;,<br>&quot;/ask&quot;,<br>&quot;/remember&quot;,<br>&quot;/memory&quot;,<br>&quot;/forget&quot;,<br>&quot;/clear_memory&quot;,<br>&quot;/exit&quot;,<br>}</p><p>@dataclass<br>class ParsedCommand:<br>&quot;&quot;&quot;A parsed command.&quot;&quot;&quot;</p><p>command: str<br>content: str</p><p>def parse_command(user_input: str) -&gt; ParsedCommand:<br>&quot;&quot;&quot;Parse user input into command and content.&quot;&quot;&quot;</p><p>cleaned = user_input.strip()</p><p>if not cleaned:<br>return ParsedCommand(command=&quot;&quot;, content=&quot;&quot;)</p><p>if not cleaned.startswith(&quot;/&quot;):<br>return ParsedCommand(command=&quot;/ask&quot;, content=cleaned)</p><pre><code>parts = cleaned.split(maxsplit=1)\n command = parts[0].lower()\n content = parts[1].strip() if len(parts) &gt; 1 else &quot;&quot;</code></pre><p>return ParsedCommand(command=command, content=content)</p><p>def is_supported_command(command: str) -&gt; bool:<br>&quot;&quot;&quot;Return True if command is supported.&quot;&quot;&quot;</p><p>return command in SUPPORTED_COMMANDS</p><p>def help_text() -&gt; str:<br>&quot;&quot;&quot;Return help text.&quot;&quot;&quot;</p><p>return &quot;&quot;&quot;<br>Available commands:</p><p>/ask [question]<br>Ask the assistant a question using saved memory when relevant.</p><p>/remember [memory]<br>Save a user-approved memory.<br>Example: /remember I prefer concise bullet-point explanations.</p><p>/memory<br>Show saved memory.</p><p>/forget [id]<br>Delete one memory by ID.<br>Example: /forget 2</p><p>/clear_memory<br>Delete all saved memory.</p><p>/help<br>Show this help message.</p><p>/exit<br>Quit the assistant.</p><p>Privacy note:<br>Do not save API keys, passwords, tokens, or sensitive private data.<br>&quot;&quot;&quot;.strip()</p><p>Test result: Python syntax check passed.</p><hr><h2>7.12 File: assistant.py</h2><p>Create assistant.py:</p><p>&quot;&quot;&quot;<br>Assistant logic for the Chapter 7 memory assistant.<br>&quot;&quot;&quot;</p><pre><code>from commands import parse_command, is_supported_command, help_text\nfrom llm_client import LLMClient, MockLLMClient\nfrom memory_store import (\n add_memory,\n list_memory,\n delete_memory,\n clear_memory,\n format_memory_for_display,\n format_memory_for_prompt,\n)\nfrom prompts import MEMORY_ASSISTANT_SYSTEM_PROMPT, build_memory_prompt</code></pre><p>def handle_remember(content: str) -&gt; str:<br>&quot;&quot;&quot;Handle /remember command.&quot;&quot;&quot;</p><pre><code>if not content.strip():\n return &quot;Please provide something to remember.&quot;</code></pre><pre><code>try:\n item = add_memory(content)\n return f&quot;Saved memory {item.id}: {item.text}&quot;\n except ValueError as error:\n return f&quot;Memory not saved: {error}&quot;</code></pre><p>def handle_forget(content: str) -&gt; str:<br>&quot;&quot;&quot;Handle /forget command.&quot;&quot;&quot;</p><pre><code>if not content.strip():\n return &quot;Please provide the memory ID to delete. Example: /forget 2&quot;</code></pre><pre><code>try:\n memory_id = int(content.strip())\n except ValueError:\n return &quot;Memory ID must be a number.&quot;</code></pre><p>deleted = delete_memory(memory_id)</p><p>if deleted:<br>return f&quot;Deleted memory {memory_id}.&quot;</p><p>return f&quot;No memory found with ID {memory_id}.&quot;</p><p>def handle_ask(content: str, client=None) -&gt; str:<br>&quot;&quot;&quot;Handle /ask command.&quot;&quot;&quot;</p><pre><code>if not content.strip():\n return &quot;Please enter a question.&quot;</code></pre><pre><code>llm = client or LLMClient()\n memory_context = format_memory_for_prompt()</code></pre><p>prompt = build_memory_prompt(<br>user_request=content,<br>memory_context=memory_context,<br>)</p><p>return llm.complete(<br>system=MEMORY_ASSISTANT_SYSTEM_PROMPT,<br>user=prompt,<br>)</p><p>def handle_command(user_input: str, client=None) -&gt; str:<br>&quot;&quot;&quot;<br>Handle one command and return a response.<br>&quot;&quot;&quot;</p><p>parsed = parse_command(user_input)</p><p>if not parsed.command:<br>return &quot;Please enter a command or question.&quot;</p><p>if parsed.command == &quot;/exit&quot;:<br>return &quot;EXIT&quot;</p><p>if parsed.command == &quot;/help&quot;:<br>return help_text()</p><p>if not is_supported_command(parsed.command):<br>return f&quot;Unknown command: {parsed.command}\\nType /help to see available commands.&quot;</p><p>if parsed.command == &quot;/remember&quot;:<br>return handle_remember(parsed.content)</p><p>if parsed.command == &quot;/memory&quot;:<br>return format_memory_for_display()</p><p>if parsed.command == &quot;/forget&quot;:<br>return handle_forget(parsed.content)</p><p>if parsed.command == &quot;/clear_memory&quot;:<br>clear_memory()<br>return &quot;All memory cleared.&quot;</p><p>if parsed.command == &quot;/ask&quot;:<br>return handle_ask(parsed.content, client=client)</p><p>return &quot;Command was recognized but not handled.&quot;</p><p>Test result: Python syntax check passed.</p><hr><h2>7.13 File: app.py</h2><p>Create app.py:</p><p>&quot;&quot;&quot;<br>Chapter 7 Project: Personal Study or Work Assistant with Memory<br>&quot;&quot;&quot;</p><p>from assistant import handle_command</p><p>def print_welcome() -&gt; None:<br>&quot;&quot;&quot;Print welcome text.&quot;&quot;&quot;</p><p>print(&quot;\\nPersonal Study or Work Assistant with Memory&quot;)<br>print(&quot;=&quot; * 60)<br>print(&quot;Type /help to see commands.&quot;)<br>print(&quot;Type /remember to save user-approved memory.&quot;)<br>print(&quot;Type /exit to quit.&quot;)<br>print(&quot;=&quot; * 60)</p><p>def main() -&gt; None:<br>&quot;&quot;&quot;Run the memory assistant.&quot;&quot;&quot;</p><p>print_welcome()</p><p>while True:<br>try:<br>user_input = input(&quot;\\nYou: &quot;).strip()</p><p>response = handle_command(user_input)</p><p>if response == &quot;EXIT&quot;:<br>print(&quot;\\nAssistant: Goodbye.&quot;)<br>break</p><p>print(&quot;\\nAssistant:&quot;)<br>print(response)</p><p>except KeyboardInterrupt:<br>print(&quot;\\n\\nAssistant: Goodbye.&quot;)<br>break</p><p>except Exception as error:<br>print(&quot;\\nAssistant:&quot;)<br>print(f&quot;Something went wrong: {error}&quot;)</p><p>if __name__ == &quot;__main__&quot;:<br>main()</p><p>Test result: Python syntax check passed.</p><hr><h2>7.14 File: test_memory_store.py</h2><p>Create test_memory_store.py:</p><p>from pathlib import Path</p><p>import pytest</p><p>from memory_store import (<br>add_memory,<br>list_memory,<br>delete_memory,<br>clear_memory,<br>format_memory_for_display,<br>format_memory_for_prompt,<br>looks_like_secret,<br>)</p><pre><code>def test_add_memory(tmp_path: Path):\n memory_file = tmp_path / &quot;memory.json&quot;</code></pre><p>item = add_memory(&quot;I prefer concise answers.&quot;, memory_file)</p><pre><code>assert item.id == 1\n assert item.text == &quot;I prefer concise answers.&quot;</code></pre><p>items = list_memory(memory_file)</p><p>assert len(items) == 1</p><pre><code>def test_add_memory_rejects_empty(tmp_path: Path):\n memory_file = tmp_path / &quot;memory.json&quot;</code></pre><p>with pytest.raises(ValueError):<br>add_memory(&quot; &quot;, memory_file)</p><pre><code>def test_add_memory_rejects_secret(tmp_path: Path):\n memory_file = tmp_path / &quot;memory.json&quot;</code></pre><p>with pytest.raises(ValueError):<br>add_memory(&quot;My API key is abcdefghijklmnop&quot;, memory_file)</p><pre><code>def test_looks_like_secret():\n assert looks_like_secret(&quot;password=abc123&quot;) is True\n assert looks_like_secret(&quot;I like short explanations.&quot;) is False</code></pre><pre><code>def test_delete_memory(tmp_path: Path):\n memory_file = tmp_path / &quot;memory.json&quot;</code></pre><p>item = add_memory(&quot;Test memory&quot;, memory_file)</p><p>deleted = delete_memory(item.id, memory_file)</p><pre><code>assert deleted is True\n assert list_memory(memory_file) == []</code></pre><pre><code>def test_delete_missing_memory(tmp_path: Path):\n memory_file = tmp_path / &quot;memory.json&quot;</code></pre><p>deleted = delete_memory(999, memory_file)</p><p>assert deleted is False</p><pre><code>def test_clear_memory(tmp_path: Path):\n memory_file = tmp_path / &quot;memory.json&quot;</code></pre><p>add_memory(&quot;Memory one&quot;, memory_file)<br>clear_memory(memory_file)</p><p>assert list_memory(memory_file) == []</p><pre><code>def test_format_memory_for_display_empty(tmp_path: Path):\n memory_file = tmp_path / &quot;memory.json&quot;</code></pre><p>assert format_memory_for_display(memory_file) == &quot;No saved memory.&quot;</p><pre><code>def test_format_memory_for_prompt(tmp_path: Path):\n memory_file = tmp_path / &quot;memory.json&quot;</code></pre><p>add_memory(&quot;I am learning Python.&quot;, memory_file)</p><p>formatted = format_memory_for_prompt(memory_file)</p><p>assert &quot;I am learning Python.&quot; in formatted</p><p>Test result: Python syntax check passed.</p><hr><h2>7.15 File: test_commands.py</h2><p>Create test_commands.py:</p><p>from commands import parse_command, is_supported_command, help_text</p><pre><code>def test_parse_command_with_slash():\n parsed = parse_command(&quot;/remember I prefer short answers.&quot;)</code></pre><pre><code>assert parsed.command == &quot;/remember&quot;\n assert parsed.content == &quot;I prefer short answers.&quot;</code></pre><pre><code>def test_parse_normal_text_defaults_to_ask():\n parsed = parse_command(&quot;Explain RAG.&quot;)</code></pre><pre><code>assert parsed.command == &quot;/ask&quot;\n assert parsed.content == &quot;Explain RAG.&quot;</code></pre><pre><code>def test_parse_empty_input():\n parsed = parse_command(&quot; &quot;)</code></pre><pre><code>assert parsed.command == &quot;&quot;\n assert parsed.content == &quot;&quot;</code></pre><pre><code>def test_supported_command():\n assert is_supported_command(&quot;/memory&quot;) is True\n assert is_supported_command(&quot;/unknown&quot;) is False</code></pre><pre><code>def test_help_text_mentions_memory_commands():\n text = help_text()</code></pre><pre><code>assert &quot;/remember&quot; in text\n assert &quot;/memory&quot; in text\n assert &quot;/forget&quot; in text\n assert &quot;/clear_memory&quot; in text</code></pre><p>Test result: Python syntax check passed.</p><hr><h2>7.16 File: test_assistant.py</h2><p>Create test_assistant.py:</p><pre><code>from assistant import handle_command\nfrom llm_client import MockLLMClient</code></pre><pre><code>def test_handle_help():\n response = handle_command(&quot;/help&quot;)</code></pre><pre><code>assert &quot;Available commands&quot; in response\n assert &quot;/remember&quot; in response</code></pre><pre><code>def test_handle_exit():\n assert handle_command(&quot;/exit&quot;) == &quot;EXIT&quot;</code></pre><pre><code>def test_handle_unknown_command():\n response = handle_command(&quot;/unknown&quot;)</code></pre><p>assert &quot;Unknown command&quot; in response</p><pre><code>def test_handle_empty_input():\n response = handle_command(&quot; &quot;)</code></pre><p>assert &quot;Please enter&quot; in response</p><pre><code>def test_handle_ask_with_mock_client():\n mock = MockLLMClient(response_text=&quot;Mock memory answer&quot;)</code></pre><p>response = handle_command(<br>&quot;/ask Explain RAG.&quot;,<br>client=mock,<br>)</p><pre><code>assert response == &quot;Mock memory answer&quot;\n assert len(mock.calls) == 1\n assert &quot;Explain RAG.&quot; in mock.calls[0][&quot;user&quot;]</code></pre><pre><code>def test_handle_forget_missing_id():\n response = handle_command(&quot;/forget&quot;)</code></pre><p>assert &quot;Please provide&quot; in response</p><pre><code>def test_handle_forget_invalid_id():\n response = handle_command(&quot;/forget abc&quot;)</code></pre><p>assert &quot;must be a number&quot; in response</p><p>Test result: Python syntax check passed.</p><hr><h2>7.17 File: README.md</h2><p>Create README.md:</p><p>## Project: Personal Study or Work Assistant with Memory</p><p>This project builds a command-line AI assistant with user-controlled memory.</p><p>## Features</p><p>- Ask questions with `/ask`<br>- Save memory with `/remember`<br>- View memory with `/memory`<br>- Delete memory with `/forget`<br>- Clear memory with `/clear_memory`<br>- Blocks obvious secrets from memory<br>- Uses saved memory as context when relevant<br>- Includes non-API tests</p><p>## Setup</p><p>python -m venv .venv</p><p>source .venv/bin/activate</p><p>pip install -r requirements.txt</p><p>cp .env.example .env</p><p>On Windows PowerShell:</p><p>python -m venv .venv</p><p>.venv\\Scripts\\Activate.ps1</p><p>pip install -r requirements.txt</p><p>Copy-Item .env.example .env</p><p>Edit `.env` and add your API key.</p><p>## Run Tests</p><p>pytest</p><p>## Run the App</p><p>python app.py</p><p>## Example Commands</p><p>/remember I prefer concise bullet-point explanations.</p><p>/ask Explain RAG.</p><p>/memory</p><p>/forget 1</p><p>/clear_memory</p><p>## Privacy Notes</p><p>- Do not save API keys, passwords, private tokens, or sensitive personal data.<br>- Memory is stored locally in `memory.json`.<br>- `memory.json` should not be committed to version control.<br>- This is a learning project, not a production privacy system.</p><hr><h2>7.18 How the Code Works</h2><p>This project has four main parts.</p><h3>Part 1: Command Parsing</h3><p>The commands.py file turns user input into a command and content.</p><p>Example:</p><p>/remember I prefer concise explanations.</p><p>becomes:</p><pre><code>command = &quot;/remember&quot;\ncontent = &quot;I prefer concise explanations.&quot;</code></pre><p>If the user types normal text without a command, it becomes /ask.</p><h3>Part 2: Memory Store</h3><p>The memory_store.py file saves memory to JSON.</p><p>It supports:</p><p>add_memory()<br>list_memory()<br>delete_memory()<br>clear_memory()</p><p>Each memory has:</p><p>id<br>text<br>created_at</p><h3>Part 3: Prompt with Memory Context</h3><p>When the user asks a question, the app formats memory for the prompt.</p><p>memory_context = format_memory_for_prompt()</p><p>Then it builds a prompt:</p><p>Saved memory:<br>- The user prefers concise explanations.</p><p>Current user request:<br>Explain RAG.</p><p>The model can use the memory if relevant.</p><h3>Part 4: User Control</h3><p>The user can inspect memory:</p><p>/memory</p><p>delete one item:</p><p>/forget 2</p><p>or clear everything:</p><p>/clear_memory</p><p>This is important.</p><p>Memory without user control is not trustworthy.</p><hr><h2>7.19 Sample User Session</h2><p>A typical session may look like this:</p><p>Personal Study or Work Assistant with Memory<br>============================================================<br>Type /help to see commands.<br>Type /remember to save user-approved memory.<br>Type /exit to quit.<br>============================================================</p><p>You: /remember I prefer concise bullet-point explanations.</p><p>Assistant:<br>Saved memory 1: I prefer concise bullet-point explanations.</p><p>You: /ask Explain RAG.</p><p>Assistant:<br>- RAG means Retrieval-Augmented Generation.<br>- It lets an AI app retrieve relevant information before answering.<br>- In Python, this often means searching documents, passing relevant chunks to an LLM, and generating an answer from that context.<br>- It is useful for PDF chatbots, knowledge assistants, and support bots.</p><p>This is sample output only.</p><p>Do not claim exact output unless you run the code and capture the real result.</p><hr><h2>7.20 Testing the Project</h2><p>Run:</p><p>pytest</p><p>Expected type of result:</p><p>21 passed</p><p>The exact number and timing may vary if you add more tests.</p><p>These tests check:</p><ul><li>Memory creation</li><li>Memory deletion</li><li>Memory clearing</li><li>Secret detection</li><li>Command parsing</li><li>Assistant routing</li><li>Mock LLM use</li></ul><p>The tests do not call the live model API.</p><p>That saves credits and keeps tests fast.</p><hr><h2>7.21 Live API Testing</h2><p>After adding your API key to .env, run:</p><p>python app.py</p><p>Try:</p><p>/remember I am learning Python AI agents for freelancing.</p><p>Then ask:</p><p>/ask What project should I build next?</p><p>The assistant should use the saved memory if relevant.</p><p>Then inspect memory:</p><p>/memory</p><p>Then delete it:</p><p>/forget 1</p><p>Then check:</p><p>/memory</p><p>This tests the full memory cycle.</p><hr><h2>7.22 Memory and Privacy</h2><p>Memory is useful, but privacy matters.</p><p>A memory assistant should not store secrets.</p><p>Bad memory:</p><p>My API key is sk-...</p><p>Good memory:</p><p>I prefer short explanations.</p><p>Bad memory:</p><p>My password is ...</p><p>Good memory:</p><p>I am studying for a Python interview.</p><p>The memory store in this chapter includes a basic secret detector.</p><p>It is not perfect.</p><p>It is a beginner safety layer.</p><p>For production systems, memory requires stronger security, user permissions, data retention rules, and privacy review.</p><hr><h2>7.23 Memory and Relevance</h2><p>Not all memory is useful for every request.</p><p>Example memory:</p><p>The user prefers concise answers.</p><p>This is useful often.</p><p>Example request:</p><p>What is RAG?</p><p>The memory helps shape the answer.</p><p>But memory like:</p><p>The user is interested in freelancing.</p><p>may not matter when the user asks:</p><p>What is 2 + 2?</p><p>The prompt tells the model:</p><p>If saved memory is not relevant, ignore it.</p><p>This is important.</p><p>Too much irrelevant memory can make answers worse.</p><hr><h2>7.24 Memory vs RAG</h2><p>Memory and RAG are related, but different.</p><table><thead><tr><th>Feature</th><th>Memory</th><th>RAG</th></tr></thead><tbody><tr><td>Stores</td><td>User preferences or context</td><td>Documents and knowledge</td></tr><tr><td>Example</td><td>“User prefers short answers”</td><td>PDF manual text</td></tr><tr><td>Used for</td><td>Personalization</td><td>Grounding answers</td></tr><tr><td>Storage</td><td>JSON, database, vector DB</td><td>Vector database or search index</td></tr><tr><td>Risk</td><td>Privacy</td><td>Wrong retrieval or sensitive docs</td></tr></tbody></table><p>In a strong assistant, you may use both.</p><p>Example:</p><p>Memory:<br>The user is a beginner.</p><p>RAG:<br>Retrieved document chunk about embeddings.</p><p>Answer:<br>A beginner-friendly explanation using the retrieved chunk.</p><p>That is a powerful pattern.</p><hr><h2>7.25 Memory and Tools</h2><p>Memory can also improve tool use.</p><p>Example memory:</p><p>The user prefers task lists with three priority levels.</p><p>When the user asks:</p><p>Create a task plan for building a PDF chatbot.</p><p>The assistant can format tasks as:</p><p>High priority<br>Medium priority<br>Low priority</p><p>Memory makes tools and workflows more personalized.</p><p>But tool actions should still be controlled by code and guardrails.</p><p>Do not let memory override safety rules.</p><hr><h2>7.26 Real-World Use Case</h2><p>A memory assistant can support many practical workflows.</p><h3>Study Assistant</h3><p>Remember:</p><p>The user is preparing for Python interviews.</p><p>Then answer coding questions with interview tips.</p><h3>Work Assistant</h3><p>Remember:</p><p>The user prefers summaries with action items.</p><p>Then summarize meeting notes in that style.</p><h3>Freelance Assistant</h3><p>Remember:</p><p>The user specializes in AI automation for small businesses.</p><p>Then draft proposals with that positioning.</p><h3>Writing Assistant</h3><p>Remember:</p><p>The user prefers a clear, professional tone.</p><p>Then rewrite content accordingly.</p><h3>Productivity Assistant</h3><p>Remember:</p><p>The user wants short daily planning suggestions.</p><p>Then create concise task plans.</p><p>Memory makes AI feel more useful because it reduces repetition.</p><hr><h2>7.27 Monetization and Portfolio Angle</h2><p>You can present this project as:</p><p><strong>Personal Study or Work Assistant with Memory</strong></p><p>Portfolio description:</p><blockquote><p>A Python command-line AI assistant with user-controlled memory, allowing users to save, inspect, delete, and clear preferences or context that the assistant can use in future responses.</p></blockquote><p>Freelance service ideas:</p><ul><li>Personal productivity assistant</li><li>Study assistant with preferences</li><li>Client-specific proposal assistant</li><li>Internal team assistant with user preferences</li><li>AI writing assistant with style memory</li><li>Learning coach with progress notes</li><li>Workflow assistant with reusable context</li></ul><p>This project shows that you understand personalization and privacy-aware design.</p><p>That is valuable.</p><hr><h2>7.28 Common Mistakes</h2><h3>Mistake 1: Remembering Everything Automatically</h3><p>Do not silently store every user message.</p><p>Use explicit memory commands.</p><h3>Mistake 2: No Delete Option</h3><p>Users should be able to delete memory.</p><p>Add /forget and /clear_memory.</p><h3>Mistake 3: Storing Secrets</h3><p>Never store API keys, passwords, or tokens.</p><h3>Mistake 4: Using All Memory All the Time</h3><p>Irrelevant memory can confuse the answer.</p><p>Tell the model to use memory only when relevant.</p><h3>Mistake 5: Committing memory.json</h3><p>Memory may contain user context.</p><p>Add it to .gitignore.</p><h3>Mistake 6: Treating Local JSON as Production Storage</h3><p>A JSON file is fine for learning.</p><p>Production apps need real databases, user isolation, and security review.</p><h3>Mistake 7: Not Testing Memory Logic</h3><p>Memory add, list, delete, and clear functions should be tested without the LLM.</p><hr><h2>7.29 Best Practices</h2><p>Use explicit /remember.</p><p>Let users inspect memory.</p><p>Let users delete memory.</p><p>Let users clear all memory.</p><p>Block obvious secrets.</p><p>Store memory outside Git.</p><p>Use memory only when relevant.</p><p>Keep memory short.</p><p>Avoid sensitive data.</p><p>Test memory operations.</p><p>Document privacy limitations.</p><hr><h2>7.30 Mini Challenges</h2><h3>Challenge 1: Add Memory Tags</h3><p>Let the user save:</p><p>/remember preference: I prefer short explanations.</p><p>Store the tag separately.</p><h3>Challenge 2: Add Memory Search</h3><p>Search memory by keyword before adding it to the prompt.</p><h3>Challenge 3: Add Memory Confirmation</h3><p>Before saving, ask:</p><p>Save this memory? yes/no</p><h3>Challenge 4: Add Memory Export</h3><p>Export memory to:</p><p>memory_export.md</p><h3>Challenge 5: Add Session History</h3><p>Keep the last three messages in memory only during the current session.</p><p>Do not save them permanently.</p><h3>Challenge 6: Connect Memory to Chapter 6 Tools</h3><p>Use memory to personalize task planner output.</p><hr><h2>7.31 Chapter Summary</h2><p>In this chapter, you built a personal study or work assistant with memory.</p><p>You learned that memory helps AI assistants become more personal and useful.</p><p>You also learned that memory creates privacy responsibilities.</p><p>Your assistant can now:</p><ul><li>Save user-approved memory</li><li>Show saved memory</li><li>Delete memory</li><li>Clear memory</li><li>Use memory as context</li><li>Block obvious secrets</li><li>Test memory logic without live API calls</li></ul><p>This is a major step toward practical AI agents.</p><p>In the next chapter, you will use your growing AI skills to automate business workflows.</p><p>You will build a freelance business automation agent that turns messy client messages into proposals, task lists, and follow-up emails.</p><hr><h2>Key Takeaways</h2><ul><li>Memory stores useful context for future interactions.</li><li>Short-term memory is recent context; long-term memory persists across sessions.</li><li>Memory should be user-controlled.</li><li>Do not silently remember everything.</li><li>Do not store secrets or sensitive credentials.</li><li>Users should be able to inspect, delete, and clear memory.</li><li>Memory can personalize answers, tools, and workflows.</li><li>A JSON memory store is good for learning but not production privacy.</li><li>Memory is a key building block for practical AI agents.</li></ul><hr><h2>Action Checklist</h2><p>Before moving on, confirm:</p><p>[ ] I understand what AI memory means.<br>[ ] I understand short-term and long-term memory.<br>[ ] I created the Chapter 7 project folder.<br>[ ] I created a virtual environment.<br>[ ] I added requirements.txt.<br>[ ] I created .env.example.<br>[ ] I created .gitignore.<br>[ ] I built memory_store.py.<br>[ ] I built commands.py.<br>[ ] I built assistant.py.<br>[ ] I built app.py.<br>[ ] I wrote tests for memory.<br>[ ] I wrote tests for commands.<br>[ ] I wrote tests for assistant routing.<br>[ ] I ran pytest.<br>[ ] I tested /remember.<br>[ ] I tested /memory.<br>[ ] I tested /forget.<br>[ ] I tested /clear_memory.<br>[ ] I understand why memory must be user-controlled.</p><hr><h2>Portfolio Note</h2><p>Add this project to your portfolio as:</p><p><strong>Personal Study or Work Assistant with Memory</strong></p><p>Description:</p><blockquote><p>A Python command-line AI assistant with user-controlled memory, allowing users to save, inspect, delete, and clear preferences or context that can personalize future AI responses.</p></blockquote><p>Suggested README tagline:</p><blockquote><p>“A privacy-aware Python AI assistant that remembers only what the user explicitly chooses to save.”</p></blockquote>",
  "content_hash": "19f9989a70a9c7073724a56498b581f01ebf532738a5b9068d9d8eaa7bce7c2b",
  "processing_status": "ready"
}
