{
  "id": "f535ea96-5d55-4ee0-beaf-9fa56b1f38b6",
  "order": 1,
  "title": "Chapter 2: LLMs, Prompts, Tokens, and APIs Without Confusion",
  "content": "<h1>Chapter 2: LLMs, Prompts, Tokens, and APIs Without Confusion</h1><h2>Opening Hook: The Model Is Not the App</h2><p>In Chapter 1, you built your first Python AI assistant: a code explainer.</p><p>That project did something powerful.</p><p>It took Python code as input, sent a carefully written prompt to an AI model, and returned a useful explanation.</p><p>But there was one problem.</p><p>The model-calling code was inside the project itself.</p><p>That is fine for your first project.</p><p>But if you keep copying API code into every new app, your projects will quickly become messy.</p><p>In this chapter, you will fix that.</p><p>You will build a reusable Python class called LLMClient.</p><p>This client will become the foundation for later projects.</p><p>Instead of rewriting API logic again and again, you will use one clean interface:</p>\n\nresponse = client.complete(\n system=&quot;You are a helpful assistant.&quot;,\n user=&quot;Explain Python lists.&quot;\n)\n\n<p>That is much better.</p><p>This chapter also clears up the confusing terms people use when talking about AI apps:</p><ul><li>LLMs</li><li>Prompts</li><li>Tokens</li><li>Context</li><li>APIs</li><li>Models</li><li>Temperature</li><li>Cost</li><li>Latency</li></ul><p>You do not need to become a machine learning researcher to build useful AI apps.</p><p>But you do need to understand these practical ideas.</p><p>By the end of this chapter, you will have a reusable LLM client that you can use throughout the rest of the book.</p><hr><h2>What You Will Learn</h2><p>By the end of this chapter, you will understand:</p><ul><li>What an LLM is at a practical level</li><li>What prompts are</li><li>What system and user instructions do</li><li>What tokens are</li><li>Why context length matters</li><li>What an API is</li><li>How model selection affects quality, speed, and cost</li><li>What temperature means</li><li>How to avoid hardcoding secrets</li><li>How to build a reusable LLM client in Python</li><li>How to test non-API logic without spending credits</li><li>How to design code that can be adapted to different AI providers</li></ul><hr><h2>Why This Chapter Matters</h2><p>Most beginner AI apps fail for one of three reasons.</p><h3>Reason 1: API Code Is Repeated Everywhere</h3><p>Beginners often copy the same API setup into every file.</p><p>That creates duplication.</p><p>When the API changes, they must update many files.</p><p>A reusable client solves this.</p><h3>Reason 2: Prompts Are Scattered Through the Code</h3><p>If prompts are hidden in random functions, the app becomes hard to improve.</p><p>A cleaner structure separates:</p>\n\nAPI client\nprompt templates\napp logic\ntests\n\n<h3>Reason 3: Cost and Errors Are Ignored</h3><p>AI apps can fail because:</p><ul><li>API keys are missing</li><li>Inputs are too long</li><li>Model names are wrong</li><li>Network calls fail</li><li>Outputs are too expensive</li><li>Prompts are unclear</li><li>The app calls the model unnecessarily</li></ul><p>This chapter introduces habits that prevent those problems.</p><hr><h2>2.1 What Is an LLM?</h2><p>LLM stands for <strong>Large Language Model</strong>.</p><p>At a practical level, an LLM is a model that accepts input text and generates output text.</p><p>You send:</p>\n\nExplain what a Python dictionary is.\n\n<p>The model returns something like:</p>\n\nA Python dictionary stores data as key-value pairs.\n\n<p>An LLM can help with:</p><ul><li>Explanation</li><li>Summarization</li><li>Rewriting</li><li>Classification</li><li>Question answering</li><li>Code assistance</li><li>Planning</li><li>Drafting</li><li>Reasoning over provided context</li></ul><p>But an LLM is not your full application.</p><p>An LLM is one part of your application.</p><p>Your Python code still needs to handle:</p><ul><li>User input</li><li>Files</li><li>API keys</li><li>Prompt construction</li><li>Tool calls</li><li>Memory</li><li>Retrieval</li><li>Validation</li><li>Error handling</li><li>Display</li><li>Deployment</li></ul><p>A useful AI app is built around the model.</p><hr><h2>2.2 What Is a Prompt?</h2><p>A prompt is the instruction you send to the model.</p><p>Simple prompt:</p>\n\nExplain Python lists.\n\n<p>Better prompt:</p>\n\nExplain Python lists for a beginner.\nUse one short example.\nAvoid advanced jargon.\n\n<p>Even better prompt:</p>\n\nYou are a friendly Python tutor.\n\nExplain Python lists for a beginner.\n\nReturn:\n1. Short definition\n2. Simple example\n3. Common mistake\n4. Beginner-friendly summary\n\n<p>The third prompt is better because it gives:</p><ul><li>Role</li><li>Audience</li><li>Task</li><li>Format</li><li>Constraints</li></ul><p>Good prompts are not magic words.</p><p>They are clear instructions.</p><hr><h2>2.3 System Instructions and User Requests</h2><p>In many AI apps, you separate instructions into two layers.</p><h3>System Instruction</h3><p>The system instruction defines the assistant’s behavior.</p><p>Example:</p>\n\nYou are a helpful Python tutor.\nBe clear, accurate, and beginner-friendly.\nDo not invent facts.\n\n<h3>User Request</h3><p>The user request contains what the user wants right now.</p><p>Example:</p>\n\nExplain the difference between a list and a tuple.\n\n<p>Together, they form the full model input.</p>\n\nSystem:\nYou are a helpful Python tutor.\n\nUser:\nExplain the difference between a list and a tuple.\n\n<p>This separation helps you keep the assistant consistent while allowing the user request to change.</p><hr><h2>2.4 What Are Tokens?</h2><p>A token is a small piece of text processed by the model.</p><p>A token may be:</p><ul><li>A word</li><li>Part of a word</li><li>Punctuation</li><li>Whitespace</li><li>A symbol</li></ul><p>For practical beginner purposes, you can think:</p><blockquote><p>More text means more tokens.</p></blockquote><p>Tokens matter because they affect:</p><ul><li>Cost</li><li>Speed</li><li>Context length</li><li>How much information fits in one request</li></ul><p>A short prompt costs less and runs faster than a very long prompt.</p><p>A RAG app that sends 20 pages of context every time will usually cost more than one that sends only the most relevant chunks.</p><p>This is why retrieval, chunking, and prompt design matter.</p><hr><h2>2.5 What Is Context Length?</h2><p>Context length means how much information the model can consider in one request.</p><p>This includes:</p>\n\nSystem instructions\n+ user message\n+ conversation history\n+ retrieved documents\n+ tool results\n+ any other prompt text\n\n<p>If you send too much information, you may hit limits or increase cost.</p><p>Even when a model supports a long context window, sending everything is not always wise.</p><p>Better practice:</p>\n\nSend only what is useful for the task.\n\n<p>That is why RAG is important.</p><p>Instead of sending an entire document, a RAG app retrieves the relevant chunks.</p><hr><h2>2.6 What Is an API?</h2><p>API stands for <strong>Application Programming Interface</strong>.</p><p>An API lets one software program communicate with another.</p><p>In this book, your Python program communicates with an AI provider.</p><p>The flow looks like this:</p>\n\nYour Python app\n ↓\nAPI request\n ↓\nAI provider\n ↓\nModel response\n ↓\nYour Python app\n\n<p>You send a request with:</p><ul><li>API key</li><li>Model name</li><li>Prompt</li><li>Settings</li></ul><p>The provider returns a response.</p><p>OpenAI’s current Python quickstart shows a basic Python API call using the official SDK, the OpenAI client, the Responses API, and response.output_text for retrieving text output. :contentReference[oaicite:0]{index=0}</p><hr><h2>2.7 Why Use a Reusable Client?</h2><p>In Chapter 1, the API call lived directly inside the project.</p><p>That is fine once.</p><p>But later, you will build many apps:</p><ul><li>Command-line assistant</li><li>RAG assistant</li><li>Tool agent</li><li>Memory assistant</li><li>Workflow agent</li><li>Research agent</li><li>Guarded assistant</li><li>Evaluation runner</li><li>Deployed capstone app</li></ul><p>You do not want to rewrite the same API code in every project.</p><p>A reusable client gives you one interface:</p>\n\nclient.complete(system=system_prompt, user=user_prompt)\n\n<p>That makes your app cleaner.</p><p>It also makes future changes easier.</p><p>If you switch model names, add logging, add retries, or adapt to another provider, you can update one client instead of many files.</p><hr><h2>2.8 Model Selection</h2><p>Different models can have different strengths.</p><p>Some may be:</p><ul><li>Faster</li><li>Cheaper</li><li>Better at reasoning</li><li>Better at coding</li><li>Better at following structured formats</li><li>Better at long context</li><li>Better for tool calling</li></ul><p>For beginner projects, choose a model that is:</p><ul><li>Available to you</li><li>Cost-effective</li><li>Good enough for learning</li><li>Supported by your provider’s current SDK</li></ul><p>This book uses a model name in .env so you can change it later:</p>\n\nOPENAI_MODEL=gpt-4.1-mini\n\n<p>Model names, pricing, capabilities, and API behavior can change over time.</p><p>Before publishing or deploying a serious app, check your provider’s current documentation.</p><hr><h2>2.9 Temperature</h2><p>Temperature is a setting that affects randomness.</p><p>Lower temperature usually gives more consistent responses.</p><p>Higher temperature usually gives more varied responses.</p><p>For technical apps, lower is usually better.</p><p>Example:</p>\n\nOPENAI_TEMPERATURE=0.2\n\n<p>Use lower temperature for:</p><ul><li>Code explanation</li><li>RAG answers</li><li>Structured output</li><li>Data extraction</li><li>Business workflows</li><li>Evaluation-sensitive apps</li></ul><p>Use higher temperature carefully for:</p><ul><li>Brainstorming</li><li>Creative writing</li><li>Naming ideas</li><li>Marketing drafts</li></ul><p>For most projects in this book, a temperature around 0.2 is a good learning default.</p><hr><h2>2.10 Cost Awareness</h2><p>AI APIs usually cost money based on usage.</p><p>Usage often depends on:</p>\n\ninput tokens\n+ output tokens\n+ model choice\n+ number of calls\n\n<p>A simple assistant might make one model call.</p><p>A multi-agent workflow might make five calls.</p><p>A RAG app might send long document context.</p><p>A tool-using agent might call the model to choose a tool, run the tool, and call the model again to explain the result.</p><p>Cost control matters.</p><p>Beginner cost-control habits:</p>\n\nUse concise prompts.\nAvoid unnecessary model calls.\nTest non-API code without calling the model.\nUse small examples while developing.\nRetrieve only relevant context.\nDo not send full documents unless needed.\nTrack calls in serious projects.\n\n<p>You will build a simple cost-estimation system in Chapter 11.</p><hr><h2>2.11 Provider Adaptability</h2><p>This book uses OpenAI-style examples because they are easy to demonstrate with Python.</p><p>But the architecture should not lock you into one provider forever.</p><p>A good AI app separates:</p>\n\nApplication logic\nfrom\nProvider-specific API logic\n\n<p>That means your app should call:</p>\n\nclient.complete(system, user)\n\n<p>instead of directly calling provider APIs everywhere.</p><p>Later, you could create:</p>\n\nOpenAIClient\nAnthropicClient\nLocalModelClient\nMockClient\n\n\nwith the same basic interface.\n\n<p>That is professional design.</p><hr><h2>2.12 Project: Reusable Python LLM Client</h2><h3>What This Project Does</h3><p>This project creates a reusable Python class for calling an LLM.</p><p>It will:</p><ol><li>Read configuration from environment variables</li><li>Check that an API key exists</li><li>Build a model input from system and user text</li><li>Send the request to the provider</li><li>Return text output</li><li>Provide a mock client for tests and examples</li><li>Include tests for non-API logic</li></ol><h3>Who This Helps</h3><p>This project helps:</p><ul><li>Python learners</li><li>AI app builders</li><li>Students creating portfolio projects</li><li>Freelancers building reusable tools</li><li>Developers who want cleaner code</li></ul><h3>What You Will Build</h3><p>Create this project structure:</p>\n\nchapter_02_llm_client/\n .env.example\n .gitignore\n requirements.txt\n llm_client.py\n demo.py\n test_llm_client.py\n README.md\n\n<hr><h2>2.13 Setup</h2><h3>Step 1: Create the Folder</h3>\n\nmkdir chapter_02_llm_client\ncd chapter_02_llm_client\n\n<h3>Step 2: Create a Virtual Environment</h3>\n\npython -m venv .venv\n\n<p>Activate it.</p><p>On macOS or Linux:</p>\n\nsource .venv/bin/activate\n\n<p>On Windows PowerShell:</p>\n\n.venv\\Scripts\\Activate.ps1\n\n<h3>Step 3: Create requirements.txt</h3>\n\nopenai\npython-dotenv\npytest\n\n<p>Install dependencies:</p>\n\npip install -r requirements.txt\n\n<h3>Step 4: Create .env.example</h3>\n\nOPENAI_API_KEY=replace_with_your_api_key\nOPENAI_MODEL=gpt-4.1-mini\nOPENAI_TEMPERATURE=0.2\n\n<p>Copy it to .env.</p><p>On macOS or Linux:</p>\n\ncp .env.example .env\n\n<p>On Windows PowerShell:</p>\n\nCopy-Item .env.example .env\n\n<p>Then edit .env and add your real API key.</p><p>Never publish your real .env file.</p><hr><h2>2.14 File: .gitignore</h2><p>Create .gitignore:</p>\n\n.env\n.venv/\n__pycache__/\n.pytest_cache/\n*.pyc\n*.log\n\n<p>This keeps secrets and local files out of version control.</p><hr><h2>2.15 File: llm_client.py</h2><p>Create llm_client.py:</p>\n\n&quot;&quot;&quot;\nChapter 2 Project: Reusable Python LLM Client\n\nThis module provides a small reusable client for calling an LLM API.\n\nSecurity notes:\n- Never hardcode API keys.\n- Store API keys in environment variables.\n- Do not commit .env files.\n&quot;&quot;&quot;\n\nimport os\nfrom dataclasses import dataclass, field\nfrom typing import Protocol\nfrom dotenv import load_dotenv\nfrom openai import OpenAI\n\nload_dotenv()\n\ndef get_env_float(name: str, default: float) -&gt; float:\n &quot;&quot;&quot;\n Read a float from an environment variable.\n\n If the variable is missing or invalid, return the default.\n &quot;&quot;&quot;\n\n raw_value = os.getenv(name)\n\n if raw_value is None:\n return default\n\n try:\n return float(raw_value)\n except ValueError:\n return default\n\n@dataclass\nclass LLMConfig:\n &quot;&quot;&quot;Configuration for the LLM client.&quot;&quot;&quot;\n\n model: str = field(\n default_factory=lambda: os.getenv(&quot;OPENAI_MODEL&quot;, &quot;gpt-4.1-mini&quot;)\n )\n temperature: float = field(\n default_factory=lambda: get_env_float(&quot;OPENAI_TEMPERATURE&quot;, 0.2)\n )\n\nclass LLMProvider(Protocol):\n &quot;&quot;&quot;\n Protocol for LLM clients.\n\n Any provider class with a complete(system, user) method can follow this interface.\n &quot;&quot;&quot;\n\n def complete(self, system: str, user: str) -&gt; str:\n &quot;&quot;&quot;Return model-generated text.&quot;&quot;&quot;\n ...\n\nclass LLMClient:\n &quot;&quot;&quot;\n Reusable client for sending prompts to an LLM.\n\n This implementation uses the OpenAI Python SDK.\n The rest of your app should depend on complete(), not provider-specific details.\n &quot;&quot;&quot;\n\n def __init__(self, config: LLMConfig | None = None):\n self.config = config or LLMConfig()\n api_key = os.getenv(&quot;OPENAI_API_KEY&quot;)\n\n if not api_key:\n raise ValueError(\n &quot;OPENAI_API_KEY is missing. Add it to your .env file.&quot;\n )\n\n self.client = OpenAI(api_key=api_key)\n\n def build_input(self, system: str, user: str) -&gt; str:\n &quot;&quot;&quot;\n Combine system and user instructions into readable model input.\n &quot;&quot;&quot;\n\n if not system.strip():\n raise ValueError(&quot;System instruction cannot be empty.&quot;)\n\n if not user.strip():\n raise ValueError(&quot;User request cannot be empty.&quot;)\n\n return f&quot;&quot;&quot;\nSystem instruction:\n{system.strip()}\n\nUser request:\n{user.strip()}\n&quot;&quot;&quot;\n\n def complete(self, system: str, user: str) -&gt; str:\n &quot;&quot;&quot;\n Send a request to the model and return text output.\n &quot;&quot;&quot;\n\n model_input = self.build_input(system=system, user=user)\n\n response = self.client.responses.create(\n model=self.config.model,\n input=model_input,\n temperature=self.config.temperature,\n )\n\n return response.output_text\n\nclass MockLLMClient:\n &quot;&quot;&quot;\n Mock client for tests and examples.\n\n This avoids live API calls when you only need predictable output.\n &quot;&quot;&quot;\n\n def __init__(self, response_text: str = &quot;Mock response&quot;):\n self.response_text = response_text\n self.calls: list[dict[str, str]] = []\n\n def complete(self, system: str, user: str) -&gt; str:\n &quot;&quot;&quot;\n Return a fixed response and record the call.\n &quot;&quot;&quot;\n\n if not system.strip():\n raise ValueError(&quot;System instruction cannot be empty.&quot;)\n\n if not user.strip():\n raise ValueError(&quot;User request cannot be empty.&quot;)\n\n self.calls.append(\n {\n &quot;system&quot;: system,\n &quot;user&quot;: user,\n }\n )\n\n return self.response_text\n\n\nNot executed automatically: requires live API credentials, network access, or external service.\n\n<p>The current OpenAI Python reference describes the Responses API as the primary API for interacting with models and shows response.output_text as a convenience for text output. :contentReference[oaicite:1]{index=1}</p><hr><h2>2.16 File: demo.py</h2><p>Create demo.py:</p>\n\n&quot;&quot;&quot;\nDemo script for the reusable LLM client.\n&quot;&quot;&quot;\n\nfrom llm_client import LLMClient\n\nSYSTEM_PROMPT = &quot;&quot;&quot;\nYou are a helpful Python tutor.\n\nExplain concepts clearly for beginner-to-intermediate learners.\nBe concise and practical.\nDo not invent facts.\n&quot;&quot;&quot;\n\ndef main() -&gt; None:\n &quot;&quot;&quot;Run a simple demo.&quot;&quot;&quot;\n\n client = LLMClient()\n\n user_question = &quot;Explain Python dictionaries in one paragraph with a simple example.&quot;\n\n response = client.complete(\n system=SYSTEM_PROMPT,\n user=user_question,\n )\n\n print(response)\n\nif __name__ == &quot;__main__&quot;:\n main()\n\n\nTest result: Python syntax check passed.\n\n<p>This is intentionally small.</p><p>The goal is to prove that the client works.</p><hr><h2>2.17 File: test_llm_client.py</h2><p>These tests check non-API logic.</p><p>They do not call the live model.</p>\n\nimport pytest\n\nfrom llm_client import (\n LLMConfig,\n MockLLMClient,\n get_env_float,\n)\n\ndef test_get_env_float_returns_default_when_missing(monkeypatch):\n monkeypatch.delenv(&quot;TEST_FLOAT&quot;, raising=False)\n\n result = get_env_float(&quot;TEST_FLOAT&quot;, 0.2)\n\n assert result == 0.2\n\ndef test_get_env_float_returns_default_when_invalid(monkeypatch):\n monkeypatch.setenv(&quot;TEST_FLOAT&quot;, &quot;not-a-number&quot;)\n\n result = get_env_float(&quot;TEST_FLOAT&quot;, 0.2)\n\n assert result == 0.2\n\ndef test_get_env_float_reads_valid_value(monkeypatch):\n monkeypatch.setenv(&quot;TEST_FLOAT&quot;, &quot;0.7&quot;)\n\n result = get_env_float(&quot;TEST_FLOAT&quot;, 0.2)\n\n assert result == 0.7\n\ndef test_llm_config_can_be_created():\n config = LLMConfig(model=&quot;test-model&quot;, temperature=0.1)\n\n assert config.model == &quot;test-model&quot;\n assert config.temperature == 0.1\n\ndef test_mock_client_returns_response():\n client = MockLLMClient(response_text=&quot;Hello from mock&quot;)\n\n response = client.complete(\n system=&quot;You are helpful.&quot;,\n user=&quot;Say hello.&quot;,\n )\n\n assert response == &quot;Hello from mock&quot;\n\ndef test_mock_client_records_calls():\n client = MockLLMClient(response_text=&quot;Mock response&quot;)\n\n client.complete(\n system=&quot;System prompt&quot;,\n user=&quot;User prompt&quot;,\n )\n\n assert len(client.calls) == 1\n assert client.calls[0][&quot;system&quot;] == &quot;System prompt&quot;\n assert client.calls[0][&quot;user&quot;] == &quot;User prompt&quot;\n\ndef test_mock_client_rejects_empty_system():\n client = MockLLMClient()\n\n with pytest.raises(ValueError):\n client.complete(system=&quot; &quot;, user=&quot;Hello&quot;)\n\ndef test_mock_client_rejects_empty_user():\n client = MockLLMClient()\n\n with pytest.raises(ValueError):\n client.complete(system=&quot;You are helpful.&quot;, user=&quot; &quot;)\n\n<hr><h2>2.18 File: README.md</h2><p>Create README.md:</p>\n\n## Project: Reusable Python LLM Client\n\nThis project creates a reusable Python client for calling an LLM API.\n\n## Features\n\n- Reads API key from environment variables\n- Reads model name from environment variables\n- Reads temperature from environment variables\n- Provides a clean `complete(system, user)` method\n- Includes a mock client for tests\n- Includes non-API unit tests\n\n## Setup\n\n\n\npython -m venv .venv\n\n<p>source .venv/bin/activate</p>\n\npip install -r requirements.txt\n\n<p>cp .env.example .env</p>\n\n\nOn Windows PowerShell:\n\n\n\npython -m venv .venv\n\n<p>.venv\\Scripts\\Activate.ps1</p>\n\npip install -r requirements.txt\n\n<p>Copy-Item .env.example .env</p>\n\n\nEdit `.env` and add your API key.\n\n## Run Tests\n\n\n\npytest\n\n\n\n## Run Demo\n\n\n\npython demo.py\n\n\n\n## Environment Variables\n\n\n<p>OPENAI_API_KEY=your_api_key\nOPENAI_MODEL=gpt-4.1-mini\nOPENAI_TEMPERATURE=0.2</p>\n\n\n## Notes\n\n- Never commit `.env`.\n- Model names and pricing may change.\n- The mock client lets you test app logic without live API calls.\n- You can adapt this pattern to other providers by creating another class with the same `complete(system, user)` method.\n\n<hr><h2>2.19 How the Code Works</h2><p>This project has four important parts.</p><h3>Part 1: Configuration</h3><p>The LLMConfig class stores model settings.</p>\n\n@dataclass\nclass LLMConfig:\n model: str\n temperature: float\n\n<p>The model and temperature are loaded from environment variables.</p><p>That means you can change:</p>\n\nOPENAI_MODEL\nOPENAI_TEMPERATURE\n\n<p>without editing Python code.</p><h3>Part 2: Provider Interface</h3><p>The LLMProvider protocol describes a simple interface:</p>\n\ndef complete(self, system: str, user: str) -&gt; str:\n ...\n\n<p>This is useful because your app can depend on a simple behavior rather than a specific provider.</p><p>Later, you could create another class that follows the same interface.</p><h3>Part 3: Real Client</h3><p>The LLMClient class calls the live API.</p><p>It checks that the API key exists.</p><p>It builds the input.</p><p>It sends the request.</p><p>It returns text.</p><p>Your app does not need to know the provider details.</p><h3>Part 4: Mock Client</h3><p>The MockLLMClient class returns a fixed response.</p><p>This is useful for tests.</p><p>Instead of spending credits or waiting for network calls, you can test your app logic with predictable output.</p><hr><h2>2.20 Sample Usage</h2><p>Once your .env file is configured, this code:</p>\n\nfrom llm_client import LLMClient\n\nclient = LLMClient()\n\nresponse = client.complete(\n system=&quot;You are a helpful Python tutor.&quot;,\n user=&quot;Explain Python dictionaries in one paragraph.&quot;,\n)\n\nprint(response)\n\n\nmight return:\n\n\nA Python dictionary is a collection that stores data as key-value pairs. Each key is used to look up its associated value. For example, {&quot;name&quot;: &quot;Asha&quot;, &quot;age&quot;: 25} stores a name and an age that can be accessed using their keys.\n\n<p>This is sample output only.</p><p>Do not claim exact output unless you run the code and capture the real result.</p><hr><h2>2.21 Testing the Project</h2><p>Run:</p>\n\npytest\n\n<p>Expected type of result:</p>\n\n8 passed\n\n<p>The exact timing may vary.</p><p>These tests do not call the live model API.</p><p>They check:</p><ul><li>Environment float parsing</li><li>Configuration creation</li><li>Mock client output</li><li>Mock client call recording</li><li>Empty input validation</li></ul><p>This is a good development habit.</p><p>You should not need a live API call to test basic Python logic.</p><hr><h2>2.22 Live API Testing</h2><p>After adding your API key to .env, run:</p>\n\npython demo.py\n\n<p>The demo should print an explanation of Python dictionaries.</p><p>If you see:</p>\n\nOPENAI_API_KEY is missing\n\n<p>then your .env file is missing or not loaded.</p><p>Check:</p>\n\n.env exists\nOPENAI_API_KEY is spelled correctly\npython-dotenv is installed\nyou are running the command from the project folder\n\n<hr><h2>2.23 Why This Client Will Help Later</h2><p>In the next chapter, you will build a command-line AI assistant.</p><p>That assistant will need to call the model for different commands:</p>\n\n/ask\n/explain\n/summarize\n/tasks\n\n<p>Without a reusable client, every command might repeat API code.</p><p>With the client, each command can simply call:</p>\n\nclient.complete(system=system_prompt, user=user_prompt)\n\n<p>Later chapters will use the same idea for:</p><ul><li>RAG answers</li><li>Tool routers</li><li>Memory-powered assistants</li><li>Workflow agents</li><li>Reviewers</li><li>Evaluation scripts</li><li>Deployed apps</li></ul><p>This is the point of reusable code.</p><p>You build it once, then benefit many times.</p><hr><h2>2.24 Provider-Agnostic Thinking</h2><p>Even though this chapter uses one provider-style implementation, the design is flexible.</p><p>Your app should not care whether the response comes from:</p>\n\nOpenAI\nAnthropic\nlocal model\nmock client\nfuture provider\n\n<p>Your app should care that the object has:</p>\n\ncomplete(system, user)\n\n<p>That means you can later create:</p>\n\nclass AnthropicClient:\n def complete(self, system: str, user: str) -&gt; str:\n ...\n\n<p>or:</p>\n\nclass LocalModelClient:\n def complete(self, system: str, user: str) -&gt; str:\n ...\n\n<p>or:</p>\n\nclass MockLLMClient:\n def complete(self, system: str, user: str) -&gt; str:\n return &quot;Mock response&quot;\n\n<p>This design makes your code more adaptable.</p><hr><h2>2.25 Common API Errors</h2><h3>Error 1: Missing API Key</h3><p>Cause:</p>\n\nOPENAI_API_KEY is not set.\n\n<p>Fix:</p>\n\nCreate .env and add your key.\n\n<h3>Error 2: Wrong Model Name</h3><p>Cause:</p>\n\nThe model name is unavailable or misspelled.\n\n<p>Fix:</p>\n\nCheck your provider’s current model list.\n\n<h3>Error 3: Package Not Installed</h3><p>Cause:</p>\n\nopenai or python-dotenv is missing.\n\n<p>Fix:</p>\n\npip install -r requirements.txt\n\n<h3>Error 4: Running From the Wrong Folder</h3><p>Cause:</p>\n\n.env is in the project folder, but you are running Python elsewhere.\n\n<p>Fix:</p>\n\ncd chapter_02_llm_client\npython demo.py\n\n<h3>Error 5: Expecting Exact Same Output Every Time</h3><p>Model output can vary.</p><p>Even with low temperature, wording may change.</p><p>Test meaning and structure, not always exact strings.</p><hr><h2>2.26 Cost-Saving Development Pattern</h2><p>Use this pattern while building AI apps:</p>\n\n1. Write prompt builder functions.\n2. Test prompt builder functions without API calls.\n3. Use a mock client for app logic.\n4. Run live API tests only after normal logic works.\n5. Capture real outputs only when preparing final publication.\n\n<p>This pattern saves credits.</p><p>It also makes debugging easier.</p><p>If your app logic is broken, do not blame the model first.</p><p>Test the normal Python parts.</p><hr><h2>2.27 Real-World Use Case</h2><p>A reusable LLM client is useful in any serious AI project.</p><p>For example, a freelance automation app might need:</p>\n\nanalyze_client_message()\ndraft_proposal()\ndraft_followup_email()\nreview_output()\n\n<p>Each function can use the same client.</p><p>A document assistant might need:</p>\n\nanswer_question_from_context()\nsummarize_document()\ngenerate_followup_questions()\n\n<p>Again, one client.</p><p>A multi-agent workflow might need:</p>\n\nplanner_agent()\nresearcher_agent()\nwriter_agent()\nreviewer_agent()\n\n<p>Again, one client.</p><p>Reusable infrastructure makes AI projects easier to maintain.</p><hr><h2>2.28 Monetization and Portfolio Angle</h2><p>You can present this project as:</p><p><strong>Reusable Python LLM Client</strong></p><p>Portfolio description:</p><blockquote><p>A clean Python wrapper for calling an LLM API using environment-based configuration, safe API-key handling, structured system/user prompts, and a mock client for testing.</p></blockquote><p>This project is not flashy by itself.</p><p>But it shows maturity.</p><p>A beginner shows a chatbot.</p><p>A stronger developer shows reusable architecture.</p><p>Clients and employers value people who can build maintainable systems.</p><hr><h2>2.29 Common Mistakes</h2><h3>Mistake 1: Copying API Calls Everywhere</h3><p>Repeated API code becomes hard to update.</p><p>Use a reusable client.</p><h3>Mistake 2: Hardcoding Secrets</h3><p>Never write real API keys into Python files.</p><h3>Mistake 3: Not Separating System and User Prompts</h3><p>Keep stable assistant behavior separate from changing user requests.</p><h3>Mistake 4: Testing Everything With Live API Calls</h3><p>Use mock clients and unit tests for normal Python logic.</p><h3>Mistake 5: Ignoring Cost</h3><p>Every model call can cost money.</p><p>Avoid unnecessary calls.</p><h3>Mistake 6: Locking Your App Too Tightly to One Provider</h3><p>Wrap provider-specific details behind your own interface.</p><h3>Mistake 7: Assuming API Behavior Never Changes</h3><p>AI APIs evolve.</p><p>Keep provider-specific code isolated and check current documentation before serious deployment.</p><hr><h2>2.30 Best Practices</h2><p>Use a .env file for local secrets.</p><p>Never commit .env.</p><p>Keep API code in one reusable client.</p><p>Use system prompts for assistant behavior.</p><p>Use user prompts for the current request.</p><p>Validate empty inputs.</p><p>Use mock clients for tests.</p><p>Keep model names configurable.</p><p>Use lower temperature for technical tasks.</p><p>Track model calls in serious apps.</p><p>Document setup steps clearly.</p><hr><h2>2.31 Mini Challenges</h2><h3>Challenge 1: Add a max_chars Input Check</h3><p>Modify LLMClient.build_input() to reject very long user input.</p><p>Example:</p>\n\nif len(user) &gt; 6000:\n raise ValueError(&quot;User request is too long.&quot;)\n\n<h3>Challenge 2: Add Basic Logging</h3><p>Log every call to:</p>\n\nllm_calls.log\n\n<p>Include:</p>\n\ntimestamp\nmodel\ninput character count\n\n<p>Do not log secrets or sensitive user data.</p><h3>Challenge 3: Add a complete_safe() Method</h3><p>Create a method that catches errors and returns a friendly message instead of raising.</p><p>Example:</p>\n\ndef complete_safe(self, system: str, user: str) -&gt; str:\n try:\n return self.complete(system, user)\n except Exception as error:\n return f&quot;LLM error: {error}&quot;\n\n<h3>Challenge 4: Add a Local Mock Demo</h3><p>Create:</p>\n\nmock_demo.py\n\n<p>Use MockLLMClient to simulate a response without an API key.</p><h3>Challenge 5: Add Provider Notes</h3><p>Add a PROVIDER_NOTES.md file explaining how another provider could be added using the same complete(system, user) interface.</p><hr><h2>2.32 Chapter Summary</h2><p>In this chapter, you learned the practical foundation of LLM-powered Python apps.</p><p>You learned what LLMs, prompts, tokens, context, APIs, models, temperature, and cost mean in application development.</p><p>You also built a reusable Python LLM client.</p><p>This client gives you a clean interface:</p>\n\nclient.complete(system, user)\n\n<p>That small design choice will make every future project easier.</p><p>In Chapter 1, you built a simple assistant.</p><p>In Chapter 2, you built reusable infrastructure.</p><p>In Chapter 3, you will use that infrastructure to create your first command-line AI application.</p><hr><h2>Key Takeaways</h2><ul><li>An LLM is one part of an AI application, not the whole app.</li><li>A prompt is a clear instruction sent to the model.</li><li>System instructions define assistant behavior.</li><li>User requests define the current task.</li><li>Tokens affect cost, speed, and context length.</li><li>APIs let your Python app communicate with AI providers.</li><li>A reusable LLM client prevents duplicated API code.</li><li>Mock clients help you test without live API calls.</li><li>Keep provider-specific details isolated.</li><li>Model names, pricing, and API behavior can change, so design for adaptability.</li></ul><hr><h2>Action Checklist</h2><p>Before moving on, confirm:</p>\n\n[ ] I understand what an LLM is.\n[ ] I understand what prompts are.\n[ ] I understand system and user instructions.\n[ ] I understand why tokens matter.\n[ ] I understand what an API does.\n[ ] I created the Chapter 2 project folder.\n[ ] I created a virtual environment.\n[ ] I added requirements.txt.\n[ ] I created .env.example.\n[ ] I created .gitignore.\n[ ] I wrote llm_client.py.\n[ ] I wrote demo.py.\n[ ] I wrote test_llm_client.py.\n[ ] I ran pytest.\n[ ] I tested the live demo with my API key.\n[ ] I understand why reusable clients matter.\n\n<hr><h2>Portfolio Note</h2><p>Add this project to your portfolio as:</p><p><strong>Reusable Python LLM Client</strong></p><p>Description:</p><blockquote><p>A reusable Python client for calling an LLM API with environment-based configuration, safe API-key handling, structured system/user prompts, and a mock client for testing app logic without live API calls.</p></blockquote><p>Suggested README tagline:</p><blockquote><p>“A clean reusable LLM client for building Python AI apps without duplicating API code.”</p></blockquote>",
  "content_hash": "01e3d13a55912c32159c5ea0ad4fd3143e99785913db447768a8324aea7c8838"
}
